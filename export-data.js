"use strict";

// export-data.js -- pure content: the ORIGINAL student/ starter files
// (byte-for-byte, generated from the real student/ tree) plus marker
// metadata (file/indent/begin/end) for every TODO. This is the splice
// BASE for the Python-export feature in app.js. It intentionally holds
// no reference answers and no todos.json content beyond marker text and
// indentation, which are already visible to students in their own files.
var EXPORT_FILES = {
  "settings.py": "\"\"\"Settings for the three-round Dijkstra maze game.\"\"\"\n\nSCREEN_WIDTH = 1040\nSCREEN_HEIGHT = 700\nFPS = 60\n# TODO 1 [Required]: Rewrite the game title and rules to match your game.\n# The current text is only an example chosen for this starter project.\n# Modify the strings below so players can understand your own game.\n# Keep quotation marks around every text value.\n#\n# Tip: You may write TODO 1 now with a rough draft, then come back and\n# rewrite MISSION_RULES / HOW_TO_PLAY_RULES again after finishing TODO 5-2\n# (the last required TODO). By then you will know your own scoring and\n# terrain rules exactly, and every student's rules will look different.\n# You may freely edit this text as much as you like, any time.\n\n# --- TODO 1: WRITE YOUR CODE BELOW ---\nTITLE = \"Maze Runner\"  # Modify this title to match your game.\nGAME_SUBTITLE = \"Collect treasures, avoid hazards, and reach the goal\"\nMISSION_RULES = [\n    \"Reach the goal before time runs out.\",\n    \"Your final score must be above 0.\",\n]\nHOW_TO_PLAY_RULES = [\n    \"Reach the goal to clear each round.\",\n    \"Move with the Arrow Keys or WASD.\",\n    \"Terrain and items can change your score.\",\n    \"Finish each round with a score above 0.\",\n]\n# --- END OF TODO 1 ---\n\n\nMAZE_OFFSET_X = 32\nMAZE_OFFSET_Y = 126\n\nSHOW_DFS_GENERATION = True\nDFS_STEPS_PER_FRAME = 8\n\nWHITE = (255, 255, 255)\nBLACK = (25, 25, 25)\nBACKGROUND_COLOR = (241, 245, 249)\nWALL_COLOR = (30, 41, 59)\nVISITED_COLOR = (186, 230, 253)\nCURRENT_CELL_COLOR = (251, 191, 36)\nPATH_COLOR = (139, 92, 246)\nSWAMP_COLOR = (134, 180, 146)\nPLAYER_COLOR = (37, 99, 235)\nGOAL_COLOR = (250, 204, 21)\nITEM_COLOR = (34, 197, 94)\nBOMB_COLOR = (15, 23, 42)\nBOMB_EXPLOSION_COLOR = (239, 68, 68)\n\n# Professional UI palette\nNAVY = (15, 23, 42)\nSLATE = (71, 85, 105)\nMUTED = (100, 116, 139)\nPANEL_COLOR = (255, 255, 255)\nPANEL_BORDER = (226, 232, 240)\nACCENT = (79, 70, 229)\nACCENT_LIGHT = (238, 242, 255)\nSUCCESS = (22, 163, 74)\nWARNING = (245, 158, 11)\nDANGER = (220, 38, 38)\n\nPLAYER_MOVE_DELAY_MS = 100\nSTARTING_SCORE = 0\nITEM_SCORE = 100\nSWAMP_SCORE_PENALTY = 100\n\nALLOW_PATH_HINT = True\nMAX_HINT_COUNT = 2\n\n# =========================================================\n# Route weights that students can customize\n# =========================================================\n# Students may freely use negative, zero, or positive values.\n# Example:\n# Item: -100   -> a cell the route should prefer\n# Normal: 0    -> a standard cell\n# Swamp: 100   -> a cell the route should avoid\n# Bomb: 1000   -> a cell the route should strongly avoid\n# Before Dijkstra runs, the same offset is added to every weight\n# so that the smallest adjusted weight becomes 1.\nSTUDENT_ITEM_WEIGHT = -100\nSTUDENT_NORMAL_WEIGHT = 0\nSTUDENT_SWAMP_WEIGHT = 100\nSTUDENT_BOMB_WEIGHT = 1000\n\n# Configuration for each round\n# Round 2 uses the original game difficulty.\n# TODO 7 [Bonus]: Redesign the three rounds.\n# Detailed hint:\n# You may change rows, cols, object counts, extra_open_walls, and time_limit_seconds.\n# Keep every dictionary key and keep all values as integers.\n# Smaller rows and cols make an easier round.\n\n# --- TODO 7: WRITE YOUR CODE BELOW ---\nROUND_CONFIGS = [\n    {\n        \"rows\": 11,\n        \"cols\": 15,\n        \"cell_size\": 38,\n        \"extra_open_walls\": 5,\n        \"item_count\": 8,\n        \"swamp_count\": 3,\n        \"bomb_count\": 2,\n        \"custom_item_count\": 2,\n        \"custom_terrain_count\": 2,\n        \"time_limit_seconds\": 70,\n    },\n    {\n        \"rows\": 15,\n        \"cols\": 21,\n        \"cell_size\": 30,\n        \"extra_open_walls\": 6,\n        \"item_count\": 10,\n        \"swamp_count\": 5,\n        \"bomb_count\": 4,\n        \"custom_item_count\": 3,\n        \"custom_terrain_count\": 3,\n        \"time_limit_seconds\": 55,\n    },\n    {\n        \"rows\": 17,\n        \"cols\": 25,\n        \"cell_size\": 25,\n        \"extra_open_walls\": 8,\n        \"item_count\": 12,\n        \"swamp_count\": 7,\n        \"bomb_count\": 6,\n        \"custom_item_count\": 4,\n        \"custom_terrain_count\": 4,\n        \"time_limit_seconds\": 45,\n    },\n]\n# --- END OF TODO 7 ---\n\n# Use the built-in shape when an image path is None.\n# TODO 8 [Bonus] (Part 1/2): Replace the player, goal, terrain, item, or bomb images.\n# Detailed hint:\n# Several ready-to-use images are provided in assets/images/ so you can pick\n# among them, or supply your own. Put an image file path in quotation marks,\n# such as \"assets/images/player_1.png\". Keep None to use the built-in fallback drawing.\n# You may change any number of the five lines below.\n\n# --- TODO 8 (Part 1/2): WRITE YOUR CODE BELOW ---\nPLAYER_IMAGE_PATH = None  # Example: \"assets/images/boy.png\" (also try lion.png, duck.png, player_ninja.png, player_robot.png)\nGOAL_IMAGE_PATH = None  # Example: \"assets/images/house.png\" (also try goal_flag.png, goal_door.png, goal_chest.png)\nSWAMP_IMAGE_PATH = None  # Example: \"assets/images/terrain_swamp_1.png\" (also try terrain_swamp_2.png, terrain_ice.png, terrain_lava.png, terrain_mud.png)\nITEM_IMAGE_PATH = None  # Example: \"assets/images/apple.png\" (also try candy.png, item_gem_1.png, item_gem_2.png, item_coin.png, item_star.png)\nBOMB_IMAGE_PATH = None  # Example: \"assets/images/bomb.png\" (also try bomb_2.png)\nFLOOR_TILE_IMAGE_PATH = None  # Background floor for open path cells. Example: \"assets/images/floor_tile_1.png\" (also try floor_tile_2.png)\n# --- END OF TODO 8 (Part 1/2) ---\nBOMB_EXPLOSION_IMAGE_PATH = None  # Example: \"assets/images/explode.png\" (also try explode_2.png)\nBOMB_EXPLOSION_DURATION_MS = 500\n\n# TODO 8 [Bonus] (Part 2/2): Add background music and sound effects.\n# Detailed hint:\n# Several ready-to-use sound effects and a background music loop are\n# provided in assets/sounds/ so you can pick among them, or supply your own.\n# Keep a value as None to run the game without that sound.\n\n# --- TODO 8 (Part 2/2): WRITE YOUR CODE BELOW ---\nSWAMP_SOUND_PATH = None  # Example: \"assets/sounds/squish_1.wav\" (also try squish_2.wav)\nITEM_SOUND_PATH = None  # Example: \"assets/sounds/pickup_1.wav\" (also try pickup_2.wav)\nBOMB_SOUND_PATH = None  # Example: \"assets/sounds/explosion_1.wav\" (also try explosion_2.wav)\nBACKGROUND_MUSIC_PATH = None  # Example: \"assets/sounds/bgm_1.wav\" (also try bgm_2.wav)\n# --- END OF TODO 8 (Part 2/2) ---\nBACKGROUND_MUSIC_VOLUME = 0.25\nCUSTOM_ITEM_IMAGE_PATH = None\nCUSTOM_ITEM_SOUND_PATH = None\nCUSTOM_TERRAIN_IMAGE_PATH = None\nCUSTOM_TERRAIN_SOUND_PATH = None\n\n# =========================================================\n# Customize the extra collectible item below.\n# =========================================================\n# TODO 9 [Bonus]: Customize the extra collectible item.\n# Detailed hint:\n# Change its name, RGB color, score, hint bonus, and Dijkstra route weight.\n# Smaller route weights make Dijkstra prefer that cell.\n# Keep the same variable names so the rest of the game continues to work.\n\n# --- TODO 9: WRITE YOUR CODE BELOW ---\nCUSTOM_ITEM_NAME = \"Custom Item\"\nCUSTOM_ITEM_COLOR = (180, 180, 180)\nCUSTOM_ITEM_SCORE = 0\nCUSTOM_ITEM_HINT_BONUS = 0\nCUSTOM_ITEM_ROUTE_WEIGHT = 0\n# --- END OF TODO 9 ---\n\n# =========================================================\n# Customize the extra terrain below.\n# =========================================================\n# TODO 10 [Bonus]: Customize the extra terrain.\n# Detailed hint:\n# Change its name, RGB color, score change, route weight, and whether it disappears.\n# Use True or False for CUSTOM_TERRAIN_DISAPPEARS.\n# Keep the same variable names so the game remains executable.\n\n# --- TODO 10: WRITE YOUR CODE BELOW ---\nCUSTOM_TERRAIN_NAME = \"Custom Terrain\"\nCUSTOM_TERRAIN_COLOR = (180, 180, 180)\nCUSTOM_TERRAIN_SCORE_CHANGE = 0\nCUSTOM_TERRAIN_ROUTE_WEIGHT = 0\nCUSTOM_TERRAIN_DISAPPEARS = False\n# --- END OF TODO 10 ---\n",
  "game.py": "import pygame\n\nfrom settings import (\n    SCREEN_WIDTH,\n    SCREEN_HEIGHT,\n    FPS,\n    TITLE,\n    GAME_SUBTITLE,\n    MISSION_RULES,\n    HOW_TO_PLAY_RULES,\n    BACKGROUND_COLOR,\n    BLACK,\n    WHITE,\n    SHOW_DFS_GENERATION,\n    DFS_STEPS_PER_FRAME,\n    PLAYER_MOVE_DELAY_MS,\n    STARTING_SCORE,\n    ITEM_SCORE,\n    SWAMP_SCORE_PENALTY,\n    ALLOW_PATH_HINT,\n    MAX_HINT_COUNT,\n    ROUND_CONFIGS,\n    SWAMP_SOUND_PATH,\n    ITEM_SOUND_PATH,\n    BOMB_SOUND_PATH,\n    BACKGROUND_MUSIC_PATH,\n    BACKGROUND_MUSIC_VOLUME,\n    CUSTOM_ITEM_SOUND_PATH,\n    CUSTOM_TERRAIN_SOUND_PATH,\n    CUSTOM_ITEM_SCORE,\n    CUSTOM_ITEM_HINT_BONUS,\n    CUSTOM_ITEM_ROUTE_WEIGHT,\n    CUSTOM_ITEM_NAME,\n    CUSTOM_ITEM_COLOR,\n    CUSTOM_TERRAIN_NAME,\n    CUSTOM_TERRAIN_COLOR,\n    CUSTOM_TERRAIN_SCORE_CHANGE,\n    CUSTOM_TERRAIN_ROUTE_WEIGHT,\n    CUSTOM_TERRAIN_DISAPPEARS,\n    STUDENT_NORMAL_WEIGHT,\n    STUDENT_ITEM_WEIGHT,\n    STUDENT_SWAMP_WEIGHT,\n    STUDENT_BOMB_WEIGHT,\n    NAVY,\n    SLATE,\n    MUTED,\n    PANEL_COLOR,\n    PANEL_BORDER,\n    ACCENT,\n    ACCENT_LIGHT,\n    SUCCESS,\n    WARNING,\n    DANGER,\n    MAZE_OFFSET_X,\n    MAZE_OFFSET_Y,\n    ITEM_COLOR,\n)\nfrom maze import Maze\nfrom cell import Cell\nfrom player import Player\nfrom goal import Goal\nfrom items import ScoreItem, CustomItem, Bomb, create_random_positions\nfrom pathfinding import find_path_dijkstra\n\n\ndef _lerp_color(c1, c2, t):\n    return tuple(int(c1[i] + (c2[i] - c1[i]) * t) for i in range(3))\n\n\ndef draw_vertical_gradient(surface, rect, top_color, bottom_color):\n    \"\"\"Fill rect with a smooth top-to-bottom color gradient.\"\"\"\n    height = max(1, rect.height)\n    band = pygame.Surface((rect.width, height))\n    for y in range(height):\n        color = _lerp_color(top_color, bottom_color, y / height)\n        pygame.draw.line(band, color, (0, y), (rect.width, y))\n    surface.blit(band, rect.topleft)\n\n\ndef draw_radial_glow(surface, center, radius, color, max_alpha=90):\n    \"\"\"Draw a soft radial glow by layering translucent circles.\"\"\"\n    glow = pygame.Surface((radius * 2, radius * 2), pygame.SRCALPHA)\n    steps = 10\n    for i in range(steps, 0, -1):\n        t = i / steps\n        r = int(radius * t)\n        alpha = int(max_alpha * (1 - t) ** 2)\n        pygame.draw.circle(glow, (*color, alpha), (radius, radius), r)\n    surface.blit(glow, (center[0] - radius, center[1] - radius), special_flags=pygame.BLEND_RGBA_ADD)\n\n\ndef draw_soft_shadow(surface, rect, radius=16, spread=14, max_alpha=50):\n    \"\"\"UI drop shadows are disabled. This function intentionally does nothing.\"\"\"\n    return\n\n\nclass Game:\n    def __init__(self):\n        pygame.init()\n\n        self.screen = pygame.display.set_mode((SCREEN_WIDTH, SCREEN_HEIGHT))\n        pygame.display.set_caption(TITLE)\n\n        self.clock = pygame.time.Clock()\n        self.font = pygame.font.SysFont(\"arial\", 22)\n        self.small_font = pygame.font.SysFont(\"arial\", 17)\n        self.tiny_font = pygame.font.SysFont(\"arial\", 15)\n        self.medium_font = pygame.font.SysFont(\"arial\", 28, bold=True)\n        self.big_font = pygame.font.SysFont(\"arial\", 42, bold=True)\n        self.hero_font = pygame.font.SysFont(\"arial\", 58, bold=True)\n\n        self.swamp_sound = self.load_sound(SWAMP_SOUND_PATH)\n        self.item_sound = self.load_sound(ITEM_SOUND_PATH)\n        self.bomb_sound = self.load_sound(BOMB_SOUND_PATH)\n        self.custom_item_sound = self.load_sound(CUSTOM_ITEM_SOUND_PATH)\n        self.custom_terrain_sound = self.load_sound(CUSTOM_TERRAIN_SOUND_PATH)\n        self.load_background_music()\n\n        self.running = True\n        self.screen_state = \"start\"\n        self.ui_buttons = []\n\n        self.current_round = 0\n        self.game_clear = False\n        self.round_failed = False\n        self.failure_reason = \"\"\n        self.round_transition_time = None\n\n        self.load_round()\n\n    def load_sound(self, path):\n        if path is None:\n            return None\n\n        try:\n            if not pygame.mixer.get_init():\n                pygame.mixer.init()\n            return pygame.mixer.Sound(path)\n        except (pygame.error, FileNotFoundError, TypeError) as error:\n            print(f\"[Info] Sound effect load failed: {error}\")\n            return None\n\n    def load_background_music(self):\n        if BACKGROUND_MUSIC_PATH is None:\n            return\n        try:\n            if not pygame.mixer.get_init():\n                pygame.mixer.init()\n            pygame.mixer.music.load(BACKGROUND_MUSIC_PATH)\n            pygame.mixer.music.set_volume(BACKGROUND_MUSIC_VOLUME)\n            pygame.mixer.music.play(-1)\n        except (pygame.error, FileNotFoundError, TypeError) as error:\n            print(f\"[Info] Background music load failed: {error}\")\n\n    def load_round(self):\n        self.config = ROUND_CONFIGS[self.current_round]\n\n        self.maze = Maze(self.config)\n        if not SHOW_DFS_GENERATION:\n            self.maze.generate_all()\n\n        self.player = Player(self.config[\"cell_size\"])\n        self.goal = Goal(\n            self.config[\"rows\"],\n            self.config[\"cols\"],\n            self.config[\"cell_size\"],\n        )\n\n        self.items = []\n        self.bombs = []\n        self.objects_created = False\n\n        self.score = STARTING_SCORE\n        self.hints_remaining = MAX_HINT_COUNT\n        self.start_time = None\n        self.last_player_move_time = 0\n        self.custom_terrain_triggered = set()\n\n        self.game_clear = False\n        self.round_failed = False\n        self.failure_reason = \"\"\n        self.round_transition_time = None\n\n    def start_game(self):\n        self.current_round = 0\n        self.load_round()\n        self.screen_state = \"playing\"\n\n    def restart_game(self):\n        self.current_round = 0\n        self.load_round()\n        self.screen_state = \"playing\"\n\n    def quit_game(self):\n        self.running = False\n\n    def create_game_objects(self):\n        forbidden = {\n            self.player.get_position(),\n            self.goal.get_position(),\n        }\n\n        for row in self.maze.grid:\n            for cell in row:\n                if cell.terrain == \"SWAMP\":\n                    forbidden.add((cell.row, cell.col))\n\n        item_positions = create_random_positions(\n            self.config[\"rows\"],\n            self.config[\"cols\"],\n            self.config[\"item_count\"],\n            forbidden,\n        )\n\n        self.items = [\n            ScoreItem(row, col, self.config[\"cell_size\"])\n            for row, col in item_positions\n        ]\n\n        forbidden.update(item_positions)\n        custom_positions = create_random_positions(\n            self.config[\"rows\"], self.config[\"cols\"],\n            self.config.get(\"custom_item_count\", 0), forbidden,\n        )\n        self.items.extend(\n            CustomItem(row, col, self.config[\"cell_size\"])\n            for row, col in custom_positions\n        )\n        forbidden.update(custom_positions)\n\n        bomb_positions = create_random_positions(\n            self.config[\"rows\"],\n            self.config[\"cols\"],\n            self.config[\"bomb_count\"],\n            forbidden,\n        )\n\n        self.bombs = [\n            Bomb(row, col, self.config[\"cell_size\"])\n            for row, col in bomb_positions\n        ]\n\n        self.objects_created = True\n        self.start_time = pygame.time.get_ticks()\n\n    # =========================================================\n    # 1. EVENT HANDLING\n    # =========================================================\n    def handle_events(self):\n        for event in pygame.event.get():\n            if event.type == pygame.QUIT:\n                self.running = False\n\n            elif event.type == pygame.MOUSEBUTTONDOWN and event.button == 1:\n                self.handle_mouse_click(event.pos)\n\n            elif event.type == pygame.KEYDOWN:\n                if self.screen_state == \"start\":\n                    if event.key in (pygame.K_RETURN, pygame.K_SPACE):\n                        self.start_game()\n                    elif event.key == pygame.K_ESCAPE:\n                        self.running = False\n                    continue\n\n                if event.key == pygame.K_ESCAPE:\n                    self.running = False\n\n                elif event.key == pygame.K_r:\n                    self.restart_game()\n\n                elif event.key in (pygame.K_RETURN, pygame.K_SPACE):\n                    if self.round_failed or self.game_clear:\n                        self.restart_game()\n\n                elif (\n                    event.key == pygame.K_h\n                    and ALLOW_PATH_HINT\n                    and self.maze.generation_complete\n                    and not self.game_clear\n                    and not self.round_failed\n                ):\n                    self.show_player_hint()\n\n    def handle_mouse_click(self, position):\n        for button in reversed(self.ui_buttons):\n            if button[\"enabled\"] and button[\"rect\"].collidepoint(position):\n                button[\"action\"]()\n                break\n\n    # =========================================================\n    # 2. UPDATE LOGIC\n    # =========================================================\n    def update(self):\n        if self.screen_state != \"playing\":\n            return\n\n        if self.round_transition_time is not None:\n            elapsed = pygame.time.get_ticks() - self.round_transition_time\n            if elapsed >= 900:\n                self.current_round += 1\n                self.load_round()\n            return\n\n        if self.game_clear or self.round_failed:\n            return\n\n        if not self.maze.generation_complete:\n            for _ in range(DFS_STEPS_PER_FRAME):\n                self.maze.generate_step()\n                if self.maze.generation_complete:\n                    break\n            return\n\n        if not self.objects_created:\n            self.create_game_objects()\n\n        self.update_player()\n        self.update_bombs()\n        self.check_items()\n        self.check_bombs()\n        self.check_swamp()\n        self.check_custom_terrain()\n        self.check_goal()\n        self.check_time_limit()\n\n    def update_player(self):\n        now = pygame.time.get_ticks()\n        if now - self.last_player_move_time < PLAYER_MOVE_DELAY_MS:\n            return\n\n        keys = pygame.key.get_pressed()\n        moved = False\n\n        # TODO 2-1 [Required]: Connect the arrow keys or WASD keys to maze movement.\n        # This is the grid-movement version of Reach the Star TODO 2.\n        # Recall the same key checks you used there:\n        # Left/A, Right/D, Up/W, and Down/S.\n        # Here, instead of changing velocity, call self.player.try_move(direction, self.maze)\n        # and store the result in moved, the same way the line below is written:\n        # moved = self.player.try_move(\"left\", self.maze)\n        # Only the direction string changes for each key (see the hints below).\n        # Keep the first condition as if and the remaining conditions as elif,\n        # so the player moves only one maze cell at a time.\n\n        # --- TODO 2-1: WRITE YOUR CODE BELOW ---\n        if keys[pygame.K_LEFT] or keys[pygame.K_a]:\n            pass  # Write your code here. Hint: the direction string is \"left\"\n        elif keys[pygame.K_RIGHT] or keys[pygame.K_d]:\n            pass  # Write your code here. Hint: the direction string is \"right\"\n        elif keys[pygame.K_UP] or keys[pygame.K_w]:\n            pass  # Write your code here. Hint: the direction string is \"top\"\n        elif keys[pygame.K_DOWN] or keys[pygame.K_s]:\n            pass  # Write your code here. Hint: the direction string is \"bottom\"\n        # --- END OF TODO 2-1 ---\n\n        if moved:\n            self.last_player_move_time = now\n            self.maze.clear_path_display()\n\n    def update_bombs(self):\n        now = pygame.time.get_ticks()\n        for bomb in self.bombs:\n            bomb.update(now)\n\n    def check_items(self):\n        player_position = self.player.get_position()\n        for item in self.items:\n            if item.active and item.get_position() == player_position:\n                item.active = False\n                if isinstance(item, CustomItem):\n                    self.score += CUSTOM_ITEM_SCORE\n                    self.hints_remaining = min(\n                        MAX_HINT_COUNT + CUSTOM_ITEM_HINT_BONUS,\n                        self.hints_remaining + CUSTOM_ITEM_HINT_BONUS,\n                    )\n                    if self.custom_item_sound:\n                        self.custom_item_sound.play()\n                else:\n                    # TODO 5-1 [Required]: Add the normal treasure score.\n                    # Detailed hint:\n                    # ITEM_SCORE contains the score for one normal treasure.\n                    # Add it to self.score with +=.\n\n                    # --- TODO 5-1: WRITE YOUR CODE BELOW ---\n                    pass  # Write your code here.\n                    # --- END OF TODO 5-1 ---\n                    if self.item_sound:\n                        self.item_sound.play()\n\n    def check_bombs(self):\n        now = pygame.time.get_ticks()\n        player_position = self.player.get_position()\n\n        for bomb in self.bombs:\n            if bomb.state == \"ACTIVE\" and bomb.get_position() == player_position:\n                if bomb.trigger(now):\n                    self.player.reset_position()\n                    self.maze.clear_path_display()\n                    if self.bomb_sound:\n                        self.bomb_sound.play()\n                break\n\n    def check_swamp(self):\n        position = self.player.get_position()\n        if self.maze.get_terrain(position) != \"SWAMP\":\n            return\n\n        self.maze.get_cell(*position).terrain = \"NORMAL\"\n        # TODO 5-2 [Required]: Subtract the swamp penalty from the score.\n        # Detailed hint:\n        # SWAMP_SCORE_PENALTY contains the amount to lose.\n        # Subtract it from self.score with -=.\n\n        # --- TODO 5-2: WRITE YOUR CODE BELOW ---\n        pass  # Write your code here.\n        # --- END OF TODO 5-2 ---\n\n        if self.swamp_sound:\n            self.swamp_sound.play()\n\n    def check_custom_terrain(self):\n        position = self.player.get_position()\n        if self.maze.get_terrain(position) != \"CUSTOM\":\n            return\n        if position in self.custom_terrain_triggered:\n            return\n        self.custom_terrain_triggered.add(position)\n        self.score += CUSTOM_TERRAIN_SCORE_CHANGE\n        if CUSTOM_TERRAIN_DISAPPEARS:\n            self.maze.get_cell(*position).terrain = \"NORMAL\"\n        if self.custom_terrain_sound:\n            self.custom_terrain_sound.play()\n\n    def check_goal(self):\n        if self.player.get_position() != self.goal.get_position():\n            return\n\n        if self.score <= 0:\n            self.round_failed = True\n            self.failure_reason = \"ROUND FAILED: Final score must be above 0.\"\n            return\n\n        if self.current_round == len(ROUND_CONFIGS) - 1:\n            self.game_clear = True\n        else:\n            self.round_transition_time = pygame.time.get_ticks()\n\n    def check_time_limit(self):\n        if self.start_time is None:\n            return\n        if self.get_remaining_time() <= 0:\n            self.round_failed = True\n            self.failure_reason = \"ROUND FAILED: Time limit exceeded.\"\n\n    def get_remaining_time(self):\n        limit = self.config[\"time_limit_seconds\"]\n        if self.start_time is None:\n            return limit\n\n        elapsed = (pygame.time.get_ticks() - self.start_time) // 1000\n        return max(0, limit - elapsed)\n\n    def get_current_route_weight(self, position):\n        active_bomb_positions = {\n            bomb.get_position()\n            for bomb in self.bombs\n            if bomb.state == \"ACTIVE\"\n        }\n        active_item_positions = {\n            item.get_position() for item in self.items\n            if item.active and not isinstance(item, CustomItem)\n        }\n        active_custom_item_positions = {\n            item.get_position() for item in self.items\n            if item.active and isinstance(item, CustomItem)\n        }\n\n        if position in active_bomb_positions:\n            return STUDENT_BOMB_WEIGHT\n        if position in active_custom_item_positions:\n            return CUSTOM_ITEM_ROUTE_WEIGHT\n        if position in active_item_positions:\n            return STUDENT_ITEM_WEIGHT\n        if self.maze.get_terrain(position) == \"SWAMP\":\n            return STUDENT_SWAMP_WEIGHT\n        if self.maze.get_terrain(position) == \"CUSTOM\":\n            return CUSTOM_TERRAIN_ROUTE_WEIGHT\n        return STUDENT_NORMAL_WEIGHT\n\n    def show_player_hint(self):\n        if self.hints_remaining <= 0:\n            return\n\n        student_weights = [\n            STUDENT_ITEM_WEIGHT,\n            STUDENT_NORMAL_WEIGHT,\n            STUDENT_SWAMP_WEIGHT,\n            STUDENT_BOMB_WEIGHT,\n            CUSTOM_ITEM_ROUTE_WEIGHT,\n            CUSTOM_TERRAIN_ROUTE_WEIGHT,\n        ]\n\n        path = find_path_dijkstra(\n            self.maze,\n            self.player.get_position(),\n            self.goal.get_position(),\n            get_weight=self.get_current_route_weight,\n            all_weights=student_weights,\n        )\n\n        if path:\n            self.maze.show_path(path)\n            self.hints_remaining -= 1\n\n    # =========================================================\n    # 3. RENDER / DRAW\n    # =========================================================\n    def draw(self):\n        self.screen.fill(BACKGROUND_COLOR)\n        self.ui_buttons = []\n\n        if self.screen_state == \"start\":\n            self.draw_start_screen()\n            pygame.display.flip()\n            return\n\n        self.draw_header()\n        self.draw_maze_panel()\n        self.draw_side_panel()\n        self.draw_footer()\n\n        self.maze.draw(self.screen)\n\n        if self.maze.generation_complete:\n            for item in self.items:\n                item.draw(self.screen)\n            for bomb in self.bombs:\n                bomb.draw(self.screen)\n            self.goal.draw(self.screen)\n            self.player.draw(self.screen)\n\n        if not self.maze.generation_complete:\n            self.ui_buttons = []\n            self.draw_status_modal(\n                \"CREATING MAZE\",\n                \"Complete TODO 3 to build the maze.\",\n                ACCENT,\n            )\n        elif self.round_transition_time is not None:\n            self.ui_buttons = []\n            self.draw_status_modal(\n                f\"ROUND {self.current_round + 1} CLEAR\",\n                \"Preparing the next challenge\",\n                SUCCESS,\n            )\n        elif self.round_failed:\n            self.ui_buttons = []\n            reason = (\n                \"Your final score must be above 0.\"\n                if self.score <= 0\n                else \"The time limit has expired.\"\n            )\n            self.draw_status_modal(\n                \"ROUND FAILED\",\n                reason,\n                DANGER,\n                help_text=\"Try again from Round 1.\",\n                buttons=[\n                    (\"Restart\", self.restart_game, \"primary\"),\n                    (\"Quit\", self.quit_game, \"secondary\"),\n                ],\n            )\n        elif self.game_clear:\n            self.ui_buttons = []\n            self.draw_status_modal(\n                \"MISSION COMPLETE\",\n                \"All 3 rounds cleared!\",\n                SUCCESS,\n                help_text=\"Great job, Maze Runner!\",\n                buttons=[\n                    (\"Play Again\", self.restart_game, \"primary\"),\n                    (\"Quit\", self.quit_game, \"secondary\"),\n                ],\n            )\n\n        pygame.display.flip()\n\n    def draw_rounded_panel(\n        self,\n        rect,\n        color=PANEL_COLOR,\n        border=PANEL_BORDER,\n        radius=16,\n        shadow=True,\n    ):\n        if shadow:\n            draw_soft_shadow(self.screen, rect, radius=radius, spread=12, max_alpha=45)\n\n        panel = pygame.Surface(rect.size, pygame.SRCALPHA)\n        draw_vertical_gradient(\n            panel,\n            pygame.Rect(0, 0, *rect.size),\n            _lerp_color(color, (255, 255, 255), 0.35),\n            color,\n        )\n        mask = pygame.Surface(rect.size, pygame.SRCALPHA)\n        pygame.draw.rect(mask, (255, 255, 255, 255), (0, 0, *rect.size), border_radius=radius)\n        panel.blit(mask, (0, 0), special_flags=pygame.BLEND_RGBA_MULT)\n\n        self.screen.blit(panel, rect.topleft)\n        pygame.draw.rect(self.screen, border, rect, width=1, border_radius=radius)\n\n    def draw_button(self, rect, text, action, style=\"primary\", enabled=True):\n        mouse_pos = pygame.mouse.get_pos()\n        hovered = enabled and rect.collidepoint(mouse_pos)\n\n        styles = {\n            \"primary\": {\n                \"fill\": ACCENT,\n                \"fill_hover\": (99, 102, 241),\n                \"border\": ACCENT,\n                \"text\": WHITE,\n            },\n            \"secondary\": {\n                \"fill\": PANEL_COLOR,\n                \"fill_hover\": (248, 250, 252),\n                \"border\": PANEL_BORDER,\n                \"text\": NAVY,\n            },\n            \"success\": {\n                \"fill\": SUCCESS,\n                \"fill_hover\": (34, 197, 94),\n                \"border\": SUCCESS,\n                \"text\": WHITE,\n            },\n            \"danger\": {\n                \"fill\": DANGER,\n                \"fill_hover\": (239, 68, 68),\n                \"border\": DANGER,\n                \"text\": WHITE,\n            },\n        }\n        palette = styles.get(style, styles[\"primary\"])\n\n        fill = palette[\"fill_hover\"] if hovered else palette[\"fill\"]\n        border = palette[\"border\"]\n        text_color = palette[\"text\"]\n\n        if not enabled:\n            fill = (226, 232, 240)\n            border = (203, 213, 225)\n            text_color = (148, 163, 184)\n\n        draw_soft_shadow(\n            self.screen, rect, radius=14, spread=10,\n            max_alpha=60 if hovered else 42,\n        )\n\n        button = pygame.Surface(rect.size, pygame.SRCALPHA)\n        draw_vertical_gradient(\n            button,\n            pygame.Rect(0, 0, *rect.size),\n            _lerp_color(fill, (255, 255, 255), 0.22),\n            _lerp_color(fill, (0, 0, 0), 0.08),\n        )\n        mask = pygame.Surface(rect.size, pygame.SRCALPHA)\n        pygame.draw.rect(mask, (255, 255, 255, 255), (0, 0, *rect.size), border_radius=14)\n        button.blit(mask, (0, 0), special_flags=pygame.BLEND_RGBA_MULT)\n        self.screen.blit(button, (rect.x, rect.y - (2 if hovered else 0)))\n\n        draw_rect = rect.move(0, -2 if hovered else 0)\n        pygame.draw.rect(self.screen, border, draw_rect, width=1, border_radius=14)\n        if hovered and enabled:\n            glow = pygame.Surface((rect.width + 6, rect.height + 6), pygame.SRCALPHA)\n            pygame.draw.rect(glow, (*fill, 60), glow.get_rect(), width=3, border_radius=16)\n            self.screen.blit(glow, (draw_rect.x - 3, draw_rect.y - 3))\n\n        label = self.small_font.render(text, True, text_color)\n        self.screen.blit(label, label.get_rect(center=draw_rect.center))\n\n        self.ui_buttons.append(\n            {\n                \"rect\": rect,\n                \"action\": action,\n                \"enabled\": enabled,\n            }\n        )\n\n    def draw_header(self):\n        header = pygame.Rect(0, 0, SCREEN_WIDTH, 98)\n        draw_vertical_gradient(self.screen, header, (23, 33, 61), NAVY)\n        for i in range(4):\n            alpha = 70 - i * 15\n            band = pygame.Surface((SCREEN_WIDTH, 1), pygame.SRCALPHA)\n            band.fill((*ACCENT, max(alpha, 0)))\n            self.screen.blit(band, (0, 94 + i))\n\n        title = self.medium_font.render(TITLE.upper(), True, WHITE)\n        subtitle = self.small_font.render(\n            GAME_SUBTITLE,\n            True,\n            (203, 213, 225),\n        )\n        self.screen.blit(title, (32, 20))\n        self.screen.blit(subtitle, (33, 57))\n\n        cards = [\n            (\"ROUND\", f\"{self.current_round + 1} / {len(ROUND_CONFIGS)}\", ACCENT),\n            (\"SCORE\", f\"{self.score:+d}\", SUCCESS if self.score > 0 else WARNING),\n            (\n                \"TIME\",\n                f\"{self.get_remaining_time()}s\",\n                DANGER if self.get_remaining_time() <= 10 else ACCENT,\n            ),\n            (\"HINTS\", f\"{self.hints_remaining} / {MAX_HINT_COUNT}\", ACCENT),\n        ]\n        x = 470\n        for label, value, color in cards:\n            rect = pygame.Rect(x, 16, 124, 64)\n            card = pygame.Surface(rect.size, pygame.SRCALPHA)\n            draw_vertical_gradient(card, pygame.Rect(0, 0, *rect.size), (38, 51, 73), (26, 36, 54))\n            mask = pygame.Surface(rect.size, pygame.SRCALPHA)\n            pygame.draw.rect(mask, (255, 255, 255, 255), (0, 0, *rect.size), border_radius=12)\n            card.blit(mask, (0, 0), special_flags=pygame.BLEND_RGBA_MULT)\n            self.screen.blit(card, rect.topleft)\n            pygame.draw.rect(self.screen, (*color, 90), rect, width=1, border_radius=12)\n            self.screen.blit(\n                self.tiny_font.render(label, True, (148, 163, 184)),\n                (x + 14, 25),\n            )\n            self.screen.blit(self.font.render(value, True, color), (x + 14, 47))\n            x += 136\n\n    def draw_maze_panel(self):\n        maze_width = self.config[\"cols\"] * self.config[\"cell_size\"]\n        maze_height = self.config[\"rows\"] * self.config[\"cell_size\"]\n        rect = pygame.Rect(\n            MAZE_OFFSET_X - 14,\n            MAZE_OFFSET_Y - 14,\n            maze_width + 28,\n            maze_height + 28,\n        )\n        self.draw_rounded_panel(rect, radius=18)\n\n    def draw_side_panel(self):\n        panel = pygame.Rect(704, 118, 304, 486)\n        self.draw_rounded_panel(panel, radius=18)\n\n        self.screen.blit(self.medium_font.render(\"Mission\", True, NAVY), (728, 140))\n        mission_lines = MISSION_RULES\n        y = 181\n        for line in mission_lines:\n            self.screen.blit(self.small_font.render(line, True, SLATE), (728, y))\n            y += 24\n\n        pygame.draw.line(self.screen, PANEL_BORDER, (728, 238), (984, 238), 1)\n        self.screen.blit(self.font.render(\"Game Elements\", True, NAVY), (728, 258))\n\n        # This list drives the \"Game Elements\" legend panel. TREASURE, SWAMP,\n        # BOMB, and GOAL describe the fixed Required game rules and are\n        # provided for you (their score values update automatically if you\n        # change ITEM_SCORE/SWAMP_SCORE_PENALTY in settings.py). You may\n        # freely edit any of the label text here as much as you like.\n        # CUSTOM_ITEM/CUSTOM_TERRAIN show your own Bonus TODO 9/10 name once\n        # you rename them in settings.py.\n        elements = [\n            (\"TREASURE\", ITEM_COLOR, \"Treasure\", f\"+{ITEM_SCORE} score\"),\n            (\"SWAMP\", (107, 151, 119), \"Swamp\", f\"-{SWAMP_SCORE_PENALTY} score\"),\n            (\"CUSTOM_ITEM\", CUSTOM_ITEM_COLOR, CUSTOM_ITEM_NAME, \"Edit after TODO 9\"),\n            (\"CUSTOM_TERRAIN\", CUSTOM_TERRAIN_COLOR, CUSTOM_TERRAIN_NAME, \"Edit after TODO 10\"),\n            (\"BOMB\", (30, 41, 59), \"Bomb\", \"Return to start\"),\n            (\"GOAL\", (250, 204, 21), \"Goal\", \"Finish the round\"),\n        ]\n        y = 300\n        for element_type, color, name, detail in elements:\n            icon_rect = pygame.Rect(733, y - 2, 22, 22)\n            if element_type == \"TREASURE\":\n                ScoreItem(0, 0, 30).draw_icon(self.screen, icon_rect)\n            elif element_type == \"SWAMP\":\n                Cell.draw_terrain_icon(self.screen, icon_rect, \"SWAMP\")\n            elif element_type == \"CUSTOM_ITEM\":\n                CustomItem(0, 0, 30).draw_icon(self.screen, icon_rect)\n            elif element_type == \"CUSTOM_TERRAIN\":\n                Cell.draw_terrain_icon(self.screen, icon_rect, \"CUSTOM\")\n            elif element_type == \"BOMB\":\n                pygame.draw.circle(self.screen, color, icon_rect.center, 8)\n                pygame.draw.circle(\n                    self.screen,\n                    WHITE,\n                    (icon_rect.centerx + 4, icon_rect.centery - 4),\n                    2,\n                )\n            else:\n                pygame.draw.rect(self.screen, color, icon_rect, border_radius=6)\n\n            self.screen.blit(self.small_font.render(name, True, NAVY), (760, y))\n            detail_surface = self.tiny_font.render(detail, True, MUTED)\n            self.screen.blit(\n                detail_surface,\n                (984 - detail_surface.get_width(), y + 2),\n            )\n            y += 30\n\n        pygame.draw.line(self.screen, PANEL_BORDER, (728, 492), (984, 492), 1)\n        self.screen.blit(self.font.render(\"Hint Route\", True, NAVY), (728, 508))\n        hint_box = pygame.Rect(728, 540, 256, 42)\n        pygame.draw.rect(self.screen, ACCENT_LIGHT, hint_box, border_radius=12)\n        key_rect = pygame.Rect(740, 546, 30, 28)\n        pygame.draw.rect(self.screen, ACCENT, key_rect, border_radius=8)\n        key = self.small_font.render(\"H\", True, WHITE)\n        self.screen.blit(key, key.get_rect(center=key_rect.center))\n        self.screen.blit(\n            self.small_font.render(\"Show Dijkstra route\", True, ACCENT),\n            (782, 551),\n        )\n\n    def draw_footer(self):\n        footer = pygame.Rect(0, 628, SCREEN_WIDTH, 72)\n        pygame.draw.rect(self.screen, (248, 250, 252), footer)\n        pygame.draw.line(self.screen, PANEL_BORDER, (0, 628), (SCREEN_WIDTH, 628), 1)\n\n        controls = [\n            (\"MOVE\", \"Arrow Keys / WASD\"),\n            (\"RESTART\", \"R\"),\n            (\"QUIT\", \"ESC\"),\n        ]\n        x = 32\n        for label, key in controls:\n            self.screen.blit(self.tiny_font.render(label, True, MUTED), (x, 646))\n            key_surface = self.small_font.render(key, True, NAVY)\n            self.screen.blit(key_surface, (x, 666))\n            x += 215\n\n        status = \"MAZE READY\" if self.maze.generation_complete else \"GENERATING\"\n        dot_color = SUCCESS if self.maze.generation_complete else WARNING\n        pygame.draw.circle(self.screen, dot_color, (608, 663), 5)\n        status_surface = self.small_font.render(status, True, SLATE)\n        self.screen.blit(status_surface, (621, 654))\n\n        hint_enabled = (\n            ALLOW_PATH_HINT\n            and self.maze.generation_complete\n            and not self.game_clear\n            and not self.round_failed\n            and self.hints_remaining > 0\n        )\n        self.draw_button(\n            pygame.Rect(700, 644, 110, 40),\n            \"Hint\",\n            self.show_player_hint,\n            style=\"primary\",\n            enabled=hint_enabled,\n        )\n        self.draw_button(\n            pygame.Rect(824, 644, 110, 40),\n            \"Restart\",\n            self.restart_game,\n            style=\"secondary\",\n            enabled=True,\n        )\n        self.draw_button(\n            pygame.Rect(948, 644, 72, 40),\n            \"Quit\",\n            self.quit_game,\n            style=\"secondary\",\n            enabled=True,\n        )\n\n    def draw_overlay(self):\n        overlay = pygame.Surface((SCREEN_WIDTH, SCREEN_HEIGHT), pygame.SRCALPHA)\n        overlay.fill((15, 23, 42, 168))\n        self.screen.blit(overlay, (0, 0))\n\n    def draw_status_modal(\n        self,\n        title,\n        subtitle,\n        accent_color,\n        help_text=None,\n        buttons=None,\n    ):\n        self.draw_overlay()\n        modal_height = 280 if buttons else 210\n        modal = pygame.Rect(250, 210, 540, modal_height)\n        self.draw_rounded_panel(\n            modal,\n            color=PANEL_COLOR,\n            border=(255, 255, 255),\n            radius=22,\n            shadow=True,\n        )\n        pygame.draw.rect(\n            self.screen,\n            accent_color,\n            (modal.x, modal.y, 8, modal.height),\n            border_radius=4,\n        )\n\n        icon_center = (modal.centerx, modal.y + 52)\n        pygame.draw.circle(self.screen, (*accent_color, 35), icon_center, 24)\n        pygame.draw.circle(self.screen, accent_color, icon_center, 8)\n\n        title_surface = self.big_font.render(title, True, NAVY)\n        self.screen.blit(\n            title_surface,\n            title_surface.get_rect(center=(modal.centerx, modal.y + 112)),\n        )\n\n        subtitle_surface = self.font.render(subtitle, True, SLATE)\n        self.screen.blit(\n            subtitle_surface,\n            subtitle_surface.get_rect(center=(modal.centerx, modal.y + 154)),\n        )\n\n        if help_text:\n            info_rect = pygame.Rect(modal.x + 82, modal.y + 176, modal.width - 164, 34)\n            pygame.draw.rect(self.screen, ACCENT_LIGHT, info_rect, border_radius=10)\n            info_surface = self.small_font.render(help_text, True, ACCENT)\n            self.screen.blit(info_surface, info_surface.get_rect(center=info_rect.center))\n\n        if buttons:\n            total_width = len(buttons) * 132 - 12\n            start_x = modal.centerx - total_width // 2\n            y = modal.bottom - 58\n            for index, (label, action, style) in enumerate(buttons):\n                rect = pygame.Rect(start_x + index * 132, y, 120, 42)\n                self.draw_button(rect, label, action, style=style, enabled=True)\n\n    def draw_start_screen(self):\n        draw_vertical_gradient(\n            self.screen,\n            pygame.Rect(0, 0, SCREEN_WIDTH, SCREEN_HEIGHT),\n            (241, 245, 249),\n            (226, 232, 240),\n        )\n\n        hero = pygame.Rect(0, 0, SCREEN_WIDTH, 220)\n        draw_vertical_gradient(self.screen, hero, (17, 26, 51), (10, 16, 36))\n        draw_radial_glow(self.screen, (920, 90), 170, (79, 70, 229), max_alpha=110)\n        draw_radial_glow(self.screen, (990, 10), 110, (56, 189, 248), max_alpha=70)\n        draw_radial_glow(self.screen, (860, 190), 130, (139, 92, 246), max_alpha=90)\n\n        self.screen.blit(\n            self.small_font.render(\"PYGAME MAZE PROJECT\", True, (191, 219, 254)),\n            (52, 48),\n        )\n        title = self.hero_font.render(TITLE.upper(), True, WHITE)\n        self.screen.blit(title, (48, 78))\n        subtitle = self.small_font.render(\n            \"Navigate a weighted maze and complete all 3 rounds.\",\n            True,\n            (226, 232, 240),\n        )\n        self.screen.blit(subtitle, (52, 146))\n\n        main_panel = pygame.Rect(44, 198, 610, 430)\n        side_panel = pygame.Rect(680, 198, 316, 430)\n        self.draw_rounded_panel(main_panel, radius=24)\n        self.draw_rounded_panel(side_panel, radius=24)\n\n        self.screen.blit(self.medium_font.render(\"How to Play\", True, NAVY), (72, 230))\n        # Keep only the information players need to start and finish the game.\n        bullets = HOW_TO_PLAY_RULES\n        y = 278\n        for line in bullets:\n            pygame.draw.circle(self.screen, ACCENT, (80, y + 8), 4)\n            self.screen.blit(self.small_font.render(line, True, SLATE), (96, y))\n            y += 29\n\n        chip_y = 510\n        chips = [\n            \"3 Rounds\",\n            \"Keyboard + Buttons\",\n            \"Weighted Pathfinding\",\n        ]\n        chip_x = 70\n        for chip in chips:\n            chip_surface = self.small_font.render(chip, True, ACCENT)\n            chip_rect = pygame.Rect(chip_x, chip_y, chip_surface.get_width() + 28, 34)\n            pygame.draw.rect(self.screen, ACCENT_LIGHT, chip_rect, border_radius=17)\n            pygame.draw.rect(self.screen, (199, 210, 254), chip_rect, width=1, border_radius=17)\n            self.screen.blit(chip_surface, (chip_rect.x + 14, chip_rect.y + 8))\n            chip_x += chip_rect.width + 12\n\n        self.screen.blit(self.font.render(\"Start Game\", True, NAVY), (72, 566))\n        self.screen.blit(\n            self.small_font.render(\"You can click the buttons or press Enter.\", True, MUTED),\n            (72, 592),\n        )\n        self.draw_button(\n            pygame.Rect(390, 556, 150, 48),\n            \"Start\",\n            self.start_game,\n            style=\"primary\",\n            enabled=True,\n        )\n        self.draw_button(\n            pygame.Rect(552, 556, 90, 48),\n            \"Quit\",\n            self.quit_game,\n            style=\"secondary\",\n            enabled=True,\n        )\n\n        self.screen.blit(self.medium_font.render(\"Quick Controls\", True, NAVY), (708, 230))\n        control_cards = [\n            (\"Move\", \"Arrow Keys / WASD\"),\n            (\"Hint\", \"H or Hint Button\"),\n            (\"Restart\", \"R or Restart Button\"),\n            (\"Quit\", \"ESC or Quit Button\"),\n        ]\n        y = 278\n        for label, value in control_cards:\n            card = pygame.Rect(706, y, 264, 64)\n            pygame.draw.rect(self.screen, (248, 250, 252), card, border_radius=14)\n            pygame.draw.rect(self.screen, PANEL_BORDER, card, width=1, border_radius=14)\n            self.screen.blit(self.tiny_font.render(label.upper(), True, MUTED), (724, y + 12))\n            self.screen.blit(self.small_font.render(value, True, NAVY), (724, y + 34))\n            y += 76\n\n        footer_note = self.tiny_font.render(\n            \"Tip: Hints show the current best Dijkstra path.\",\n            True,\n            MUTED,\n        )\n        self.screen.blit(footer_note, (708, 588))\n\n    # =========================================================\n    # STANDARD GAME LOOP\n    # =========================================================\n    def run(self):\n        while self.running:\n            self.handle_events()\n            self.update()\n            self.draw()\n            self.clock.tick(FPS)\n\n        pygame.quit()\n",
  "player.py": "import pygame\n\nfrom settings import MAZE_OFFSET_X, MAZE_OFFSET_Y, PLAYER_COLOR, PLAYER_IMAGE_PATH\n\n\nclass Player:\n    _images = {}\n\n    def __init__(self, cell_size, row=0, col=0):\n        self.cell_size = cell_size\n        self.row = row\n        self.col = col\n\n    @classmethod\n    def get_image(cls, cell_size):\n        if cell_size in cls._images:\n            return cls._images[cell_size]\n        if PLAYER_IMAGE_PATH is None:\n            cls._images[cell_size] = None\n            return None\n        try:\n            image = pygame.image.load(PLAYER_IMAGE_PATH).convert_alpha()\n            image = pygame.transform.scale(image, (cell_size - 10, cell_size - 10))\n        except (pygame.error, FileNotFoundError, TypeError) as error:\n            print(f\"[Info] Player image load failed: {error}\")\n            image = None\n        cls._images[cell_size] = image\n        return image\n\n    def get_position(self):\n        return self.row, self.col\n\n    def reset_position(self):\n        self.row = 0\n        self.col = 0\n\n    def try_move(self, direction, maze):\n        current = maze.get_cell(self.row, self.col)\n        # TODO 2-2 [Required]: Stop movement when there is no cell or a wall blocks the direction.\n        # Detailed hint:\n        # current can be None, and current.walls[direction] is True when a wall exists.\n        # Join the two conditions with or, then return False inside the if block.\n\n        # --- TODO 2-2: WRITE YOUR CODE BELOW ---\n        pass  # Write your code here.\n        # --- END OF TODO 2-2 ---\n        dr, dc = {\n            \"top\": (-1, 0),\n            \"right\": (0, 1),\n            \"bottom\": (1, 0),\n            \"left\": (0, -1),\n        }[direction]\n        # TODO 2-3 [Required]: Update the player's row and column.\n        # Detailed hint:\n        # dr is the row change and dc is the column change.\n        # Add dr to self.row and dc to self.col with +=.\n\n        # --- TODO 2-3: WRITE YOUR CODE BELOW ---\n        pass  # Write your code here.\n        # --- END OF TODO 2-3 ---\n        return True\n\n    def get_rect(self):\n        return pygame.Rect(\n            MAZE_OFFSET_X + self.col * self.cell_size + 5,\n            MAZE_OFFSET_Y + self.row * self.cell_size + 5,\n            self.cell_size - 10,\n            self.cell_size - 10,\n        )\n\n    def draw(self, screen):\n        rect = self.get_rect()\n        image = self.get_image(self.cell_size)\n        if image:\n            screen.blit(image, rect)\n        else:\n            pygame.draw.circle(screen, PLAYER_COLOR, rect.center, max(5, (self.cell_size - 12) // 2))\n",
  "maze.py": "import random\n\nfrom cell import Cell\nfrom pathfinding import find_path_bfs\nfrom settings import (\n    STUDENT_NORMAL_WEIGHT, STUDENT_SWAMP_WEIGHT,\n    CUSTOM_TERRAIN_ROUTE_WEIGHT,\n)\n\n\nclass Maze:\n    def __init__(self, config):\n        self.rows = config[\"rows\"]\n        self.cols = config[\"cols\"]\n        self.cell_size = config[\"cell_size\"]\n        self.extra_open_walls = config[\"extra_open_walls\"]\n        self.swamp_count = config[\"swamp_count\"]\n        self.custom_terrain_count = config.get(\"custom_terrain_count\", 0)\n\n        self.grid = [\n            [Cell(r, c, self.cell_size) for c in range(self.cols)]\n            for r in range(self.rows)\n        ]\n        self.stack = []\n        self.current = self.grid[0][0]\n        self.current.visited = True\n        self.current.is_current = True\n        self.generation_complete = False\n        self.post_processing_complete = False\n\n    def get_cell(self, row, col):\n        if 0 <= row < self.rows and 0 <= col < self.cols:\n            return self.grid[row][col]\n        return None\n\n    def get_unvisited_neighbors(self, cell):\n        candidates = [\n            (\"top\", cell.row - 1, cell.col),\n            (\"right\", cell.row, cell.col + 1),\n            (\"bottom\", cell.row + 1, cell.col),\n            (\"left\", cell.row, cell.col - 1),\n        ]\n        return [\n            (direction, neighbor)\n            for direction, row, col in candidates\n            if (neighbor := self.get_cell(row, col)) is not None\n            and not neighbor.visited\n        ]\n\n    def remove_wall_between(self, current, next_cell, direction):\n        opposite = {\n            \"top\": \"bottom\",\n            \"right\": \"left\",\n            \"bottom\": \"top\",\n            \"left\": \"right\",\n        }\n        current.walls[direction] = False\n        next_cell.walls[opposite[direction]] = False\n\n    def generate_step(self):\n        if self.generation_complete:\n            return\n\n        neighbors = self.get_unvisited_neighbors(self.current)\n        if neighbors:\n            # TODO 3-1 [Required] (COMPLETE): Randomly choose one unvisited neighbor.\n            # Detailed hint:\n            # neighbors contains pairs in the form (direction, next_cell).\n            # Use random.choice(neighbors) and unpack the result into two variables.\n\n            # --- TODO 3-1: WRITE YOUR CODE BELOW ---\n            direction, next_cell = random.choice(neighbors)\n            # --- END OF TODO 3-1 ---\n            self.current.is_current = False\n            self.stack.append(self.current)  # Given: push the current cell so we can backtrack to it later.\n            # TODO 3-2 [Required] (COMPLETE): Open the wall between the current cell and the next cell.\n            # Detailed hint:\n            # Call self.remove_wall_between with self.current, next_cell, direction.\n\n            # --- TODO 3-2: WRITE YOUR CODE BELOW ---\n            self.remove_wall_between(self.current, next_cell, direction)\n            # --- END OF TODO 3-2 ---\n            # TODO 3-3 [Required] (COMPLETE): Move to the chosen cell and mark it as visited.\n            # Detailed hint:\n            # Assign next_cell to self.current.\n            # Then set self.current.visited to True.\n\n            # --- TODO 3-3: WRITE YOUR CODE BELOW ---\n            self.current = next_cell\n            self.current.visited = True\n            # --- END OF TODO 3-3 ---\n            self.current.is_current = True\n        elif self.stack:\n            self.current.is_current = False\n            self.current = self.stack.pop()  # Given: backtrack to the most recently saved cell.\n            self.current.is_current = True\n        else:\n            self.current.is_current = False\n            self.generation_complete = True\n            self.finish_generation()\n\n    def generate_all(self):\n        while not self.generation_complete:\n            self.generate_step()\n\n    def finish_generation(self):\n        if self.post_processing_complete:\n            return\n        self.open_extra_walls(self.extra_open_walls)\n        self.create_swamps()\n        self.create_custom_terrain()\n        self.post_processing_complete = True\n\n    def open_extra_walls(self, count):\n        candidates = []\n        for row in range(self.rows):\n            for col in range(self.cols):\n                cell = self.grid[row][col]\n                if col + 1 < self.cols and cell.walls[\"right\"]:\n                    candidates.append((cell, self.grid[row][col + 1], \"right\"))\n                if row + 1 < self.rows and cell.walls[\"bottom\"]:\n                    candidates.append((cell, self.grid[row + 1][col], \"bottom\"))\n        random.shuffle(candidates)\n        for current, neighbor, direction in candidates[:count]:\n            self.remove_wall_between(current, neighbor, direction)\n\n    def create_swamps(self):\n        start = (0, 0)\n        goal = (self.rows - 1, self.cols - 1)\n        base_path = find_path_bfs(self, start, goal)\n        candidates = list(base_path[2:-2])\n        random.shuffle(candidates)\n\n        selected = candidates[:self.swamp_count]\n        if len(selected) < self.swamp_count:\n            forbidden = {start, goal, *selected}\n            others = [\n                (r, c)\n                for r in range(self.rows)\n                for c in range(self.cols)\n                if (r, c) not in forbidden\n            ]\n            random.shuffle(others)\n            selected.extend(others[: self.swamp_count - len(selected)])\n\n        for row, col in selected:\n            self.grid[row][col].terrain = \"SWAMP\"\n\n\n    def create_custom_terrain(self):\n        forbidden = {(0, 0), (self.rows - 1, self.cols - 1)}\n        candidates = [\n            (r, c)\n            for r in range(self.rows)\n            for c in range(self.cols)\n            if (r, c) not in forbidden and self.grid[r][c].terrain == \"NORMAL\"\n        ]\n        random.shuffle(candidates)\n        for row, col in candidates[:self.custom_terrain_count]:\n            self.grid[row][col].terrain = \"CUSTOM\"\n\n    def get_terrain(self, position):\n        cell = self.get_cell(*position)\n        return cell.terrain if cell else \"NORMAL\"\n\n    def get_route_weight(self, position):\n        terrain = self.get_terrain(position)\n        if terrain == \"SWAMP\":\n            return STUDENT_SWAMP_WEIGHT\n        if terrain == \"CUSTOM\":\n            return CUSTOM_TERRAIN_ROUTE_WEIGHT\n        return STUDENT_NORMAL_WEIGHT\n\n    def get_open_neighbors(self, position):\n        row, col = position\n        cell = self.get_cell(row, col)\n        if cell is None:\n            return []\n        directions = [\n            (\"top\", -1, 0),\n            (\"right\", 0, 1),\n            (\"bottom\", 1, 0),\n            (\"left\", 0, -1),\n        ]\n        result = []\n        for direction, dr, dc in directions:\n            if not cell.walls[direction]:\n                neighbor = self.get_cell(row + dr, col + dc)\n                if neighbor:\n                    result.append((neighbor.row, neighbor.col))\n        return result\n\n    def clear_path_display(self):\n        for row in self.grid:\n            for cell in row:\n                cell.is_path = False\n\n    def show_path(self, path):\n        self.clear_path_display()\n        for row, col in path:\n            cell = self.get_cell(row, col)\n            if cell:\n                cell.is_path = True\n\n    def draw(self, screen):\n        for row in self.grid:\n            for cell in row:\n                cell.draw(screen, show_generation=not self.generation_complete)\n",
  "pathfinding.py": "from collections import deque\nimport heapq\n\n\ndef find_path_bfs(map_data, start, end):\n    \"\"\"Breadth-first search: finds the route with the fewest steps.\n\n    NOTE: queue.popleft() / queue.append() (the deque operations) are\n    already written for you below. You have not learned queue/stack data\n    structures yet, so only the dictionary bookkeeping is left as a TODO.\n    A separate \"visited\" set (also given) guarantees this function always\n    finishes even if the TODO below is left blank, so an unfinished TODO 4\n    cannot freeze the game.\n    \"\"\"\n    queue = deque([start])\n    parent = {start: None}\n    visited = {start}  # Given: guarantees BFS always terminates safely.\n\n    while queue:\n        current = queue.popleft()  # Given: take the oldest position out of the queue.\n        if current == end:\n            break\n        for neighbor in map_data.get_open_neighbors(current):\n            if neighbor not in visited:\n                visited.add(neighbor)  # Given: mark this neighbor as queued.\n                # TODO 4 [Required]: Record where this neighbor came from.\n                # Detailed hint:\n                # parent is a dictionary used to rebuild the final path later.\n                # Use neighbor as the key and current as the value,\n                # the same way a normal dictionary assignment works: my_dict[key] = value.\n\n                # --- TODO 4: WRITE YOUR CODE BELOW ---\n                pass  # Write your code here.\n                # --- END OF TODO 4 ---\n                queue.append(neighbor)  # Given: add the neighbor to the queue.\n\n    return reconstruct_path(parent, end)\n\n\ndef find_path_dijkstra(\n    map_data,\n    start,\n    end,\n    get_weight=None,\n    all_weights=None,\n):\n    \"\"\"\n    This Dijkstra implementation supports student-defined negative weights.\n\n    1. Students choose weights freely, such as -100, 0, and 100.\n    2. The smallest weight is identified.\n    3. The same offset is added to every weight so the smallest value becomes 1.\n    4. Dijkstra runs using the adjusted positive weights.\n\n    Example:\n        Original weights: -100, 0, 100, 1000\n        Minimum value: -100\n        Offset: 1 - (-100) = 101\n        Adjusted weights: 1, 101, 201, 1101\n\n    NOTE: heapq.heappop() / heapq.heappush() (the priority-queue operations)\n    are already written for you below. You have not learned heap data\n    structures yet, so only the cost bookkeeping is left as a TODO.\n    A separate \"visited\" set (also given) guarantees this function always\n    finishes even if the TODO below is left blank, so an unfinished\n    Bonus TODO cannot freeze the game.\n    \"\"\"\n    if get_weight is None:\n        get_weight = map_data.get_route_weight\n\n    if all_weights is None:\n        all_weights = [0]\n\n    minimum_weight = min(all_weights)\n    weight_shift = 1 - minimum_weight\n\n    def get_positive_weight(position):\n        original_weight = get_weight(position)\n        return original_weight + weight_shift\n\n    queue = [(0, start)]\n    distance = {start: 0}\n    parent = {start: None}\n    visited = set()  # Given: guarantees Dijkstra always terminates safely.\n\n    while queue:\n        cost, current = heapq.heappop(queue)  # Given: take the lowest-cost entry out of the queue.\n\n        if current in visited:\n            continue  # Given: this entry is a stale duplicate, skip it.\n        visited.add(current)  # Given: this position is now finalized.\n\n        if current == end:\n            break\n\n        for neighbor in map_data.get_open_neighbors(current):\n            if neighbor in visited:\n                continue\n            step_cost = get_positive_weight(neighbor)\n\n            if step_cost <= 0:\n                raise ValueError(\n                    \"Adjusted Dijkstra weight must be positive.\"\n                )\n\n            # TODO 6-1 [Bonus]: Calculate the cost to reach this neighbor.\n            # Detailed hint:\n            # Add the cost already used to reach current (the variable \"cost\")\n            # and this step's cost (the variable \"step_cost\").\n            # Store the result in new_cost.\n\n            # --- TODO 6-1: WRITE YOUR CODE BELOW ---\n            new_cost = 0  # Write your code here.\n            # --- END OF TODO 6-1 ---\n\n            if (\n                neighbor not in distance\n                or new_cost < distance[neighbor]\n            ):\n                # TODO 6-2 [Bonus]: Save the better distance and its parent.\n                # Detailed hint:\n                # Set distance[neighbor] to new_cost.\n                # Set parent[neighbor] to current.\n\n                # --- TODO 6-2: WRITE YOUR CODE BELOW ---\n                pass  # Write your code here.\n                # --- END OF TODO 6-2 ---\n\n                heapq.heappush(queue, (new_cost, neighbor))  # Given: push the improved route.\n\n    return reconstruct_path(parent, end)\n\n\ndef reconstruct_path(parent, end):\n    if end not in parent:\n        return []\n    path = []\n    current = end\n    while current is not None:\n        path.append(current)\n        current = parent[current]\n    path.reverse()\n    return path\n",
  "cell.py": "import pygame\n\nfrom settings import (\n    MAZE_OFFSET_X,\n    MAZE_OFFSET_Y,\n    WALL_COLOR,\n    VISITED_COLOR,\n    CURRENT_CELL_COLOR,\n    PATH_COLOR,\n    SWAMP_COLOR,\n    SWAMP_IMAGE_PATH,\n    CUSTOM_TERRAIN_COLOR,\n    CUSTOM_TERRAIN_IMAGE_PATH,\n    FLOOR_TILE_IMAGE_PATH,\n)\n\n\nclass Cell:\n    _swamp_images = {}\n    _custom_terrain_images = {}\n    _floor_images = {}\n\n    def __init__(self, row, col, cell_size):\n        self.row = row\n        self.col = col\n        self.cell_size = cell_size\n        self.visited = False\n        self.is_current = False\n        self.is_path = False\n        self.terrain = \"NORMAL\"\n        self.walls = {\n            \"top\": True,\n            \"right\": True,\n            \"bottom\": True,\n            \"left\": True,\n        }\n\n    @classmethod\n    def get_swamp_image(cls, cell_size):\n        if cell_size in cls._swamp_images:\n            return cls._swamp_images[cell_size]\n        if SWAMP_IMAGE_PATH is None:\n            cls._swamp_images[cell_size] = None\n            return None\n        try:\n            image = pygame.image.load(SWAMP_IMAGE_PATH).convert_alpha()\n            image = pygame.transform.scale(image, (cell_size, cell_size))\n        except (pygame.error, FileNotFoundError, TypeError) as error:\n            print(f\"[Info] Swamp image load failed: {error}\")\n            image = None\n        cls._swamp_images[cell_size] = image\n        return image\n\n\n    @classmethod\n    def get_custom_terrain_image(cls, cell_size):\n        if cell_size in cls._custom_terrain_images:\n            return cls._custom_terrain_images[cell_size]\n        if CUSTOM_TERRAIN_IMAGE_PATH is None:\n            cls._custom_terrain_images[cell_size] = None\n            return None\n        try:\n            image = pygame.image.load(CUSTOM_TERRAIN_IMAGE_PATH).convert_alpha()\n            image = pygame.transform.scale(image, (cell_size, cell_size))\n        except (pygame.error, FileNotFoundError, TypeError) as error:\n            print(f\"[Info] Custom terrain image load failed: {error}\")\n            image = None\n        cls._custom_terrain_images[cell_size] = image\n        return image\n\n    @classmethod\n    def get_floor_image(cls, cell_size):\n        if cell_size in cls._floor_images:\n            return cls._floor_images[cell_size]\n        if FLOOR_TILE_IMAGE_PATH is None:\n            cls._floor_images[cell_size] = None\n            return None\n        try:\n            image = pygame.image.load(FLOOR_TILE_IMAGE_PATH).convert_alpha()\n            image = pygame.transform.scale(image, (cell_size, cell_size))\n        except (pygame.error, FileNotFoundError, TypeError) as error:\n            print(f\"[Info] Floor image load failed: {error}\")\n            image = None\n        cls._floor_images[cell_size] = image\n        return image\n\n    @classmethod\n    def draw_terrain_icon(cls, screen, rect, terrain):\n        if terrain == \"SWAMP\":\n            image = cls.get_swamp_image(rect.width)\n            fallback_color = SWAMP_COLOR\n        else:\n            image = cls.get_custom_terrain_image(rect.width)\n            fallback_color = CUSTOM_TERRAIN_COLOR\n\n        if image:\n            screen.blit(image, image.get_rect(center=rect.center))\n        else:\n            pygame.draw.rect(screen, fallback_color, rect, border_radius=6)\n\n    def get_rect(self):\n        return pygame.Rect(\n            MAZE_OFFSET_X + self.col * self.cell_size,\n            MAZE_OFFSET_Y + self.row * self.cell_size,\n            self.cell_size,\n            self.cell_size,\n        )\n\n    def draw(self, screen, show_generation=False):\n        rect = self.get_rect()\n\n        floor_image = self.get_floor_image(self.cell_size)\n        if floor_image:\n            screen.blit(floor_image, rect)\n\n        if self.terrain == \"SWAMP\":\n            image = self.get_swamp_image(self.cell_size)\n            if image:\n                screen.blit(image, rect)\n            else:\n                pygame.draw.rect(screen, SWAMP_COLOR, rect)\n\n        elif self.terrain == \"CUSTOM\":\n            image = self.get_custom_terrain_image(self.cell_size)\n            if image:\n                screen.blit(image, rect)\n            else:\n                pygame.draw.rect(screen, CUSTOM_TERRAIN_COLOR, rect)\n\n        if self.is_path:\n            overlay = pygame.Surface((self.cell_size, self.cell_size), pygame.SRCALPHA)\n            overlay.fill((*PATH_COLOR, 155))\n            screen.blit(overlay, rect)\n            pygame.draw.circle(screen, PATH_COLOR, rect.center, max(3, self.cell_size // 7))\n        elif self.is_current:\n            pygame.draw.rect(screen, CURRENT_CELL_COLOR, rect)\n        elif self.visited and show_generation:\n            overlay = pygame.Surface((self.cell_size, self.cell_size), pygame.SRCALPHA)\n            overlay.fill((*VISITED_COLOR, 110))\n            screen.blit(overlay, rect)\n\n        x, y, s = rect.x, rect.y, self.cell_size\n        if self.walls[\"top\"]:\n            pygame.draw.line(screen, WALL_COLOR, (x, y), (x + s, y), 3)\n        if self.walls[\"right\"]:\n            pygame.draw.line(screen, WALL_COLOR, (x + s, y), (x + s, y + s), 3)\n        if self.walls[\"bottom\"]:\n            pygame.draw.line(screen, WALL_COLOR, (x, y + s), (x + s, y + s), 3)\n        if self.walls[\"left\"]:\n            pygame.draw.line(screen, WALL_COLOR, (x, y), (x, y + s), 3)\n",
  "goal.py": "import pygame\n\nfrom settings import MAZE_OFFSET_X, MAZE_OFFSET_Y, GOAL_COLOR, GOAL_IMAGE_PATH\n\n\nclass Goal:\n    _images = {}\n\n    def __init__(self, rows, cols, cell_size):\n        self.row = rows - 1\n        self.col = cols - 1\n        self.cell_size = cell_size\n\n    @classmethod\n    def get_image(cls, cell_size):\n        if cell_size in cls._images:\n            return cls._images[cell_size]\n        if GOAL_IMAGE_PATH is None:\n            cls._images[cell_size] = None\n            return None\n        try:\n            image = pygame.image.load(GOAL_IMAGE_PATH).convert_alpha()\n            image = pygame.transform.scale(image, (cell_size - 8, cell_size - 8))\n        except (pygame.error, FileNotFoundError, TypeError) as error:\n            print(f\"[Info] Goal image load failed: {error}\")\n            image = None\n        cls._images[cell_size] = image\n        return image\n\n    def get_position(self):\n        return self.row, self.col\n\n    def get_rect(self):\n        return pygame.Rect(\n            MAZE_OFFSET_X + self.col * self.cell_size + 4,\n            MAZE_OFFSET_Y + self.row * self.cell_size + 4,\n            self.cell_size - 8,\n            self.cell_size - 8,\n        )\n\n    def draw(self, screen):\n        rect = self.get_rect()\n        image = self.get_image(self.cell_size)\n        if image:\n            screen.blit(image, rect)\n        else:\n            pygame.draw.rect(screen, GOAL_COLOR, rect, border_radius=max(3, self.cell_size // 5))\n",
  "items.py": "import random\nimport pygame\n\nfrom settings import (\n    MAZE_OFFSET_X,\n    MAZE_OFFSET_Y,\n    ITEM_COLOR,\n    ITEM_IMAGE_PATH,\n    BOMB_COLOR,\n    BOMB_EXPLOSION_COLOR,\n    BOMB_IMAGE_PATH,\n    BOMB_EXPLOSION_IMAGE_PATH,\n    BOMB_EXPLOSION_DURATION_MS,\n    CUSTOM_ITEM_COLOR,\n    CUSTOM_ITEM_IMAGE_PATH,\n)\n\n\nclass ScoreItem:\n    _images = {}\n\n    def __init__(self, row, col, cell_size):\n        self.row = row\n        self.col = col\n        self.cell_size = cell_size\n        self.active = True\n\n    @classmethod\n    def get_image(cls, cell_size):\n        if cell_size in cls._images:\n            return cls._images[cell_size]\n        if ITEM_IMAGE_PATH is None:\n            cls._images[cell_size] = None\n            return None\n        try:\n            image = pygame.image.load(ITEM_IMAGE_PATH).convert_alpha()\n            image = pygame.transform.scale(image, (cell_size - 8, cell_size - 8))\n        except (pygame.error, FileNotFoundError, TypeError) as error:\n            print(f\"[Info] Item image load failed: {error}\")\n            image = None\n        cls._images[cell_size] = image\n        return image\n\n    def get_position(self):\n        return self.row, self.col\n\n    def get_rect(self):\n        return pygame.Rect(\n            MAZE_OFFSET_X + self.col * self.cell_size + 4,\n            MAZE_OFFSET_Y + self.row * self.cell_size + 4,\n            self.cell_size - 8,\n            self.cell_size - 8,\n        )\n\n    def draw_collectible(self, screen, rect):\n        \"\"\"Prettier fallback item: a glossy gem with a sparkle.\"\"\"\n        shadow = pygame.Surface((rect.width + 8, rect.height + 8), pygame.SRCALPHA)\n        pygame.draw.ellipse(\n            shadow,\n            (15, 23, 42, 35),\n            (2, rect.height - 2, rect.width, max(6, rect.height // 4)),\n        )\n        screen.blit(shadow, (rect.x - 4, rect.y - 4))\n\n        cx, cy = rect.center\n        w = rect.width\n        h = rect.height\n        gem_points = [\n            (cx, rect.y + max(1, h // 10)),\n            (rect.right - max(2, w // 5), cy),\n            (cx, rect.bottom - max(1, h // 10)),\n            (rect.x + max(2, w // 5), cy),\n        ]\n\n        outline = (6, 95, 70)\n        main = ITEM_COLOR\n        light = (134, 239, 172)\n        highlight = (220, 252, 231)\n\n        pygame.draw.polygon(screen, outline, gem_points)\n        inner_points = [\n            (cx, rect.y + max(3, h // 7)),\n            (rect.right - max(4, w // 4), cy),\n            (cx, rect.bottom - max(3, h // 7)),\n            (rect.x + max(4, w // 4), cy),\n        ]\n        pygame.draw.polygon(screen, main, inner_points)\n\n        top_triangle = [\n            (cx, rect.y + max(3, h // 7)),\n            (rect.right - max(5, w // 3), cy - max(1, h // 8)),\n            (rect.x + max(5, w // 3), cy - max(1, h // 8)),\n        ]\n        pygame.draw.polygon(screen, light, top_triangle)\n\n        facet_line_y = cy\n        pygame.draw.line(screen, outline, (cx, rect.y + max(2, h // 8)), (cx, rect.bottom - max(2, h // 8)), 1)\n        pygame.draw.line(screen, outline, (rect.x + max(4, w // 4), facet_line_y), (rect.right - max(4, w // 4), facet_line_y), 1)\n\n        sparkle_radius = max(1, rect.width // 10)\n        sparkle_center = (rect.x + max(6, w // 3), rect.y + max(5, h // 3))\n        pygame.draw.circle(screen, highlight, sparkle_center, sparkle_radius)\n        pygame.draw.line(screen, highlight, (sparkle_center[0] - sparkle_radius * 2, sparkle_center[1]), (sparkle_center[0] + sparkle_radius * 2, sparkle_center[1]), 1)\n        pygame.draw.line(screen, highlight, (sparkle_center[0], sparkle_center[1] - sparkle_radius * 2), (sparkle_center[0], sparkle_center[1] + sparkle_radius * 2), 1)\n\n    def draw_icon(self, screen, rect):\n        image = self.get_image(rect.width + 8)\n        if image:\n            screen.blit(image, image.get_rect(center=rect.center))\n        else:\n            self.draw_collectible(screen, rect)\n\n    def draw(self, screen):\n        if not self.active:\n            return\n        self.draw_icon(screen, self.get_rect())\n\n\nclass CustomItem(ScoreItem):\n    _custom_images = {}\n\n    @classmethod\n    def get_image(cls, cell_size):\n        if cell_size in cls._custom_images:\n            return cls._custom_images[cell_size]\n        if CUSTOM_ITEM_IMAGE_PATH is None:\n            cls._custom_images[cell_size] = None\n            return None\n        try:\n            image = pygame.image.load(CUSTOM_ITEM_IMAGE_PATH).convert_alpha()\n            image = pygame.transform.scale(image, (cell_size - 8, cell_size - 8))\n        except (pygame.error, FileNotFoundError, TypeError) as error:\n            print(f\"[Info] Custom item image load failed: {error}\")\n            image = None\n        cls._custom_images[cell_size] = image\n        return image\n\n    def draw_collectible(self, screen, rect):\n        pygame.draw.circle(screen, CUSTOM_ITEM_COLOR, rect.center, max(5, rect.width // 3))\n        pygame.draw.circle(screen, (255, 255, 255), rect.center, max(2, rect.width // 8))\n\n\nclass Bomb:\n    _normal_images = {}\n    _explosion_images = {}\n\n    def __init__(self, row, col, cell_size):\n        self.row = row\n        self.col = col\n        self.cell_size = cell_size\n        self.state = \"ACTIVE\"\n        self.explosion_start_time = None\n\n    @classmethod\n    def load_image(cls, path, cell_size, explosion=False):\n        cache = cls._explosion_images if explosion else cls._normal_images\n        if cell_size in cache:\n            return cache[cell_size]\n        if path is None:\n            cache[cell_size] = None\n            return None\n        try:\n            image = pygame.image.load(path).convert_alpha()\n            image = pygame.transform.scale(image, (cell_size - 8, cell_size - 8))\n        except (pygame.error, FileNotFoundError, TypeError) as error:\n            print(f\"[Info] Bomb image load failed: {error}\")\n            image = None\n        cache[cell_size] = image\n        return image\n\n    def get_position(self):\n        return self.row, self.col\n\n    def get_rect(self):\n        return pygame.Rect(\n            MAZE_OFFSET_X + self.col * self.cell_size + 4,\n            MAZE_OFFSET_Y + self.row * self.cell_size + 4,\n            self.cell_size - 8,\n            self.cell_size - 8,\n        )\n\n    def trigger(self, current_time):\n        if self.state != \"ACTIVE\":\n            return False\n        self.state = \"EXPLODING\"\n        self.explosion_start_time = current_time\n        return True\n\n    def update(self, current_time):\n        if (\n            self.state == \"EXPLODING\"\n            and current_time - self.explosion_start_time >= BOMB_EXPLOSION_DURATION_MS\n        ):\n            self.state = \"INACTIVE\"\n\n    def draw(self, screen):\n        if self.state == \"INACTIVE\":\n            return\n\n        rect = self.get_rect()\n        if self.state == \"EXPLODING\":\n            image = self.load_image(BOMB_EXPLOSION_IMAGE_PATH, self.cell_size, explosion=True)\n            if image:\n                screen.blit(image, rect)\n            else:\n                pygame.draw.circle(screen, BOMB_EXPLOSION_COLOR, rect.center, max(5, self.cell_size // 3))\n            return\n\n        image = self.load_image(BOMB_IMAGE_PATH, self.cell_size, explosion=False)\n        if image:\n            screen.blit(image, rect)\n        else:\n            pygame.draw.circle(screen, BOMB_COLOR, rect.center, max(3, self.cell_size // 6))\n\n\ndef create_random_positions(rows, cols, count, forbidden):\n    candidates = [\n        (r, c)\n        for r in range(rows)\n        for c in range(cols)\n        if (r, c) not in forbidden\n    ]\n    random.shuffle(candidates)\n    return candidates[:count]\n",
  "main.py": "from game import Game\n\n\ndef main():\n    Game().run()\n\n\nif __name__ == \"__main__\":\n    main()\n",
  "requirements.txt": "pygame>=2.5.0\n"
};

// Each entry: [stepId, file, partIndex(or null), indent, beginMarker, endMarker]
var EXPORT_MARKERS = [
  [
    "1",
    "settings.py",
    null,
    "",
    "# --- TODO 1: WRITE YOUR CODE BELOW ---",
    "# --- END OF TODO 1 ---"
  ],
  [
    "2-1",
    "game.py",
    null,
    "        ",
    "# --- TODO 2-1: WRITE YOUR CODE BELOW ---",
    "# --- END OF TODO 2-1 ---"
  ],
  [
    "2-2",
    "player.py",
    null,
    "        ",
    "# --- TODO 2-2: WRITE YOUR CODE BELOW ---",
    "# --- END OF TODO 2-2 ---"
  ],
  [
    "2-3",
    "player.py",
    null,
    "        ",
    "# --- TODO 2-3: WRITE YOUR CODE BELOW ---",
    "# --- END OF TODO 2-3 ---"
  ],
  [
    "4",
    "pathfinding.py",
    null,
    "                ",
    "# --- TODO 4: WRITE YOUR CODE BELOW ---",
    "# --- END OF TODO 4 ---"
  ],
  [
    "5-1",
    "game.py",
    null,
    "                    ",
    "# --- TODO 5-1: WRITE YOUR CODE BELOW ---",
    "# --- END OF TODO 5-1 ---"
  ],
  [
    "5-2",
    "game.py",
    null,
    "        ",
    "# --- TODO 5-2: WRITE YOUR CODE BELOW ---",
    "# --- END OF TODO 5-2 ---"
  ],
  [
    "6-1",
    "pathfinding.py",
    null,
    "            ",
    "# --- TODO 6-1: WRITE YOUR CODE BELOW ---",
    "# --- END OF TODO 6-1 ---"
  ],
  [
    "6-2",
    "pathfinding.py",
    null,
    "                ",
    "# --- TODO 6-2: WRITE YOUR CODE BELOW ---",
    "# --- END OF TODO 6-2 ---"
  ],
  [
    "7",
    "settings.py",
    null,
    "",
    "# --- TODO 7: WRITE YOUR CODE BELOW ---",
    "# --- END OF TODO 7 ---"
  ],
  [
    "8",
    "settings.py",
    0,
    "",
    "# --- TODO 8 (Part 1/2): WRITE YOUR CODE BELOW ---",
    "# --- END OF TODO 8 (Part 1/2) ---"
  ],
  [
    "8",
    "settings.py",
    1,
    "",
    "# --- TODO 8 (Part 2/2): WRITE YOUR CODE BELOW ---",
    "# --- END OF TODO 8 (Part 2/2) ---"
  ],
  [
    "9",
    "settings.py",
    null,
    "",
    "# --- TODO 9: WRITE YOUR CODE BELOW ---",
    "# --- END OF TODO 9 ---"
  ],
  [
    "10",
    "settings.py",
    null,
    "",
    "# --- TODO 10: WRITE YOUR CODE BELOW ---",
    "# --- END OF TODO 10 ---"
  ]
];

window.EXPORT_DATA = { EXPORT_FILES: EXPORT_FILES, EXPORT_MARKERS: EXPORT_MARKERS };
