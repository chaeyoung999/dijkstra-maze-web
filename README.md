# Dijkstra Maze — TODO Quest (웹 버전) 안내

학생들이 `student/` 폴더 5개 파일을 직접 뒤져서 TODO를 찾는 대신, 한 페이지짜리
웹사이트에서 16개 TODO를 한 단계씩 순서대로 풀 수 있도록 만든 결과물입니다.

**2차 작업**에서 시각화 8종 + Play 탭 게임을, **3차 작업**에서 맵 에디터(TODO 7)와
에셋 업로드(TODO 8)를 완전히 구현했습니다. 학생이 Grade를 누르거나(또는 각
시각화 패널의 "Run my code" 버튼을 눌러서 오답 상태에서도) 실제로 자기 코드를
Pyodide로 실행해서 나온 결과를 애니메이션으로 보여줍니다 — 미리 만들어둔
"정답 애니메이션"이 아니라, 진짜 자기 코드가 만든 결과입니다.

**4차(최종) 작업(이번 업데이트)에서 세 가지를 추가했습니다:**
1. **"Download my project"** — 학생의 답안을 실제로 실행되는 파이썬
   프로젝트로 되돌려주는 기능 (zip 다운로드 / 파일 하나만 다운로드 /
   연결된 폴더에 바로 쓰기).
2. **배포 패키징 점검** — 이 폴더 하나만으로 완결되는지 최종 확인.
3. **`교사용/` 폴더** — 정답 해설, 수업 운영안, 체크리스트 (한국어).
   **`dijkstra_maze_web/` 바깥, `c:/Users/손채영/Desktop/kazh/pygame/교사용/`
   에 따로 만들었습니다** — `로컬서버_실행.bat`이 `dijkstra_maze_web/`
   폴더만 서빙하므로, 이 폴더 안에 있으면 학생이 브라우저로 그 주소를
   추측해서 열어볼 위험이 있기 때문입니다. 학생에게는 `dijkstra_maze_web/`
   폴더만 배포하세요.

## 1. 실행 방법

1. `dijkstra_maze_web` 폴더를 통째로 학생에게 배포합니다 (5개 파일 + 폴더 자체).
2. **`로컬서버_실행.bat`을 더블클릭**하면:
   - PC에 설치된 파이썬으로 `python -m http.server 8000`을 실행하고
   - 자동으로 브라우저에서 `http://localhost:8000`을 엽니다.
   - 창을 닫으면 서버가 꺼지므로, 작업하는 동안 창을 열어두라고 안내됩니다.
3. 파이썬이 PC에 없으면 배치 파일이 설치 안내 메시지를 띄웁니다
   (python.org에서 설치 시 "Add python.exe to PATH" 체크 필요).
4. `index.html`을 그냥 더블클릭해서 `file://`로 열어도 페이지는 뜨지만,
   상단에 "로컬 서버로 다시 실행해 달라"는 경고 배너가 뜹니다. 채점 자체는
   Pyodide(CDN)를 쓰기 때문에 file://에서도 동작은 하지만, 이후 작업(파일
   업로드 등 File System Access API)은 반드시 `http://localhost`가 필요하므로
   지금부터 로컬 서버 사용을 권장/습관화하는 것이 좋습니다.
5. 인터넷 연결이 필요합니다 (Pyodide를 CDN에서 최초 1회 내려받음, 이후에는
   브라우저 캐시를 씀).

## 2. 파일 구성

| 파일 | 역할 |
|---|---|
| `index.html` | 페이지 뼈대(HTML). 실제 내용은 전부 `app.js`가 그려 넣습니다. (4차 업데이트에서 "Download my project" 버튼과 배너 문구 추가) |
| `styles.css` | 전체 디자인, 라이트/다크 테마, 반응형 레이아웃 + 시각화/게임 UI + 맵 에디터·에셋 피커 UI + **export 모달 스타일(4차 추가)**. |
| `data.js` | 16개 TODO의 **콘텐츠**만 담은 순수 데이터. (수정 안 함, 1차 작업 그대로 재사용) |
| `export-data.js` | **(4차 신규)** `student/` 원본 5개 파일(TODO 있는 파일)의 **스타터 상태 그대로**를 담은 콘텐츠 전용 데이터 + 각 TODO의 마커 위치(파일/들여쓰기/시작-끝 마커)만 담고 있습니다. 정답은 전혀 없습니다 — Python export 기능의 "붙여넣기 베이스"입니다. |
| `app.js` | 실제 동작 전부: 단계 진행 로직, 코드 에디터, Pyodide 로딩, 채점 엔진, 진행 상황 저장/불러오기, 8개 시각화, Play 탭 게임 엔진, `mapEditor`/`assetPicker`, 그리고 **이번 4차 업데이트에서 추가된**: 프로젝트 export(zip 생성기, 파일별 다운로드, 폴더에 바로 쓰기), ast.parse 검증, HOW_TO_RUN.txt 생성기. |
| `assets/images/`, `assets/sounds/` | `student/assets`에서 그대로 복사한 스프라이트/효과음. Play 탭, 일부 시각화, **그리고 zip 다운로드에 포함되는 프로젝트 assets/**에서 모두 사용합니다 (아래 "assets 중복" 설명 참고). |
| `로컬서버_실행.bat` | 로컬 서버 실행 + 브라우저 자동 오픈. |
| `README.md` | 이 문서 (교사용, 학생에게는 배포할 필요 없음). |

> **`교사용/` 폴더는 이 폴더(`dijkstra_maze_web/`) 안에 없습니다.** 학생에게
> 배포하는 폴더와 절대 섞이지 않도록 `dijkstra_maze_web/`의 **바깥**,
> `c:/Users/손채영/Desktop/kazh/pygame/교사용/`에 따로 있습니다. 12절 참고.

## 3. 채점은 어떻게 동작하나요?

- 브라우저에서 [Pyodide](https://pyodide.org)(파이썬을 브라우저 안에서 돌리는
  런타임, WebAssembly 기반)를 CDN에서 불러옵니다. 첫 채점 시도 시 로딩하며,
  로딩 중에는 안내 배너와 버튼 비활성화로 표시됩니다. 로딩 실패 시 채점
  버튼이 비활성화되고 에러 배너가 뜹니다 (조용히 넘어가지 않음).
- **behaviour 모드 (11개 TODO)**: 학생 코드를 그대로 가져다가, 격리된 미니
  테스트 환경(가짜 Cell/Maze/Player 등)에 끼워 넣고 **행동(behaviour)만** 비교
  합니다. 변수 이름이 다르거나 코드 구조가 달라도 "행동이 같으면" 통과합니다
  (요구사항의 핵심). 단, TODO 3-1은 `direction`/`next_cell`, TODO 6-1은
  `new_cost`라는 **이름 자체**가 필요합니다(이후 단계가 그 이름을 그대로
  쓰기 때문). DFS/BFS/Dijkstra 관련 하네스는 5000 스텝 등 하드 캡을 걸어서,
  학생 코드가 잘못돼도 브라우저가 멈추지 않도록 했습니다.
- **syntax 모드 (5개 TODO: 1, 7, 8, 9, 10)**: 정답이 하나가 아닌 자유 설계
  문제라서, `ast.parse`로 문법을 확인한 뒤 "이 게임이 실행 가능한 모양인가"
  (타입, 범위, 필수 변수명, 딕셔너리 키 보존 등)만 검사합니다.
- 에러 메시지는 항상 "무엇이 맞았고 무엇이 틀렸는지"를 구체적으로 알려주도록
  작성했습니다 (`✓ ... / ✗ ...` 형태). 파이썬 문법 오류(SyntaxError 등)는
  로직 검사보다 먼저, 줄 번호와 함께 표시됩니다.
- **채점 하네스(테스트 코드)는 정답을 담고 있지 않습니다.** 정답과 비교하는
  게 아니라, 몇 가지 입력值에 대해 기대되는 "결과"만 확인합니다. 예를 들어
  TODO 5-1은 "점수가 0에서 시작해서 n개 모으면 n×ITEM_SCORE가 되는가"만
  확인하지, `self.score += ITEM_SCORE`라는 코드 자체를 담고 있지 않습니다.

## 4. 이번 작업 검증 방법 (참고용)

이 작업 환경에는 Node.js나 브라우저가 없어서, 대신 다음과 같이 검증했습니다:
- 로컬에 있는 Python 인터프리터로, `app.js`가 만들어내는 **모든 채점용
  파이썬 코드**를 실제 파일에서 그대로 추출해 실행해 보고, 정답/오답 예시
  각각에 대해 pass/fail이 올바르게 나오는지 16개 TODO 전부 확인했습니다.
  이 과정에서 실제 버그 1건(문법 모드 채점기가 `base64` 모듈을 import하지
  않은 문제)을 발견해 수정했습니다.
- JS 파일 전체의 괄호/따옴표 짝이 맞는지 별도 스크립트로 확인했습니다.
- 2차 작업(시각화 8종 + Play 탭)에서 새로 추가한 9개의 트레이스용 파이썬
  하네스도 동일한 방식으로, 실제 `app.js` 텍스트에서 그대로 추출해 정답/오답
  샘플로 실행 검증했습니다. 이 과정에서도 진짜 버그 여러 건(트레이스
  하네스 5개가 `base64` import 누락, 자동 실패 메시지 2곳이 정답 코드를
  그대로 노출)을 발견해 전부 수정했습니다.
- 3차 작업(맵 에디터 + 에셋 피커)은 새 파이썬 하네스가 없는 대신(TODO 7/8은
  원래부터 syntax 채점이라 정답이 없음), 맵 생성 알고리즘(클러스터
  생성·연결성 보장)과 타일→간선벽 변환 로직을 파이썬으로 그대로 포팅해서
  실제 3라운드 크기를 포함한 5가지 크기/시드 조합 전부 "완전 연결 +
  목표·보물 전부 도달 가능"을 확인했습니다.
- 그래도 실제 브라우저(Chrome 등)에서 Pyodide가 정상적으로 로드되고,
  에디터 자동완성/Tab/Ctrl+Enter, 캔버스 애니메이션, Play 탭 조작감,
  **File System Access API 권한 흐름/IndexedDB 재연결/드래그앤드롭 업로드**
  등 UI 동작이 매끄러운지는 **아직 실제 브라우저로 직접 열어서 확인해보지
  않았습니다.** 처음 여실 때 개발자 도구
  콘솔에 에러가 없는지, 특히 TODO 7/8 화면과 Play 탭을 한 번씩 눌러보시는
  걸 권장합니다.

## 5. 시각화 8종 + Play 탭 (이번 업데이트)

**핵심 원칙: 애니메이션을 절대 가짜로 만들지 않습니다.** 모든 시각화와 Play
탭의 게임 로직(이동, 미로 생성, BFS, 다익스트라, 점수 계산, 힌트 경로)은
학생이 **지금 저장한 실제 코드**를 Pyodide 하네스에 끼워 실행하고, 그 결과를
JSON "trace"(이벤트 목록)로 받아 캔버스에 애니메이션으로 재생합니다. 오답이면
진짜 오답 결과가 나옵니다 (예: TODO 2-2가 틀리면 벽을 그냥 통과, TODO 3-2가
틀리면 벽이 하나도 안 열림, TODO 4가 틀리면 경로가 비어 있음 등).

- `titleCard` (TODO 1): 학생의 TITLE/GAME_SUBTITLE/MISSION_RULES/
  HOW_TO_PLAY_RULES를 실제 타이틀 화면처럼 실시간으로 미리보기.
- `playerMove` (TODO 2-1/2-2/2-3): 고정된 작은 미로 위에서 캔버스를 클릭한
  뒤 방향키/WASD로 움직이면 실제 try_move 로직이 실행됩니다. 벽 통과 횟수,
  마지막 방향, moved/try_move 반환값을 실시간 표시.
- `dfsGenerate` (TODO 3-1/3-2/3-3): 셀 단위 미로 생성 애니메이션.
  Step/Run/Pause/Reset/속도(1x·2x·4x), 스택 깊이/방문 수/뚫린 벽 수 표시,
  "미로가 전부 연결됨 ✓/✗" 최종 판정. 생성 스텝은 2000회로 하드 캡.
- `bfsFlood` (TODO 4): BFS 파도(웨이브프론트) 확산 + 최종 경로. 경로 길이 vs
  최적 길이, "늪이 최단 경로를 따라 배치됨 ✓/무작위 배치 ✗" 데모 패널.
- `scoreBoard` (TODO 5-1/5-2/9/10): 타일을 하나씩 재생하며 실제 점수 변화
  확인. 커스텀 아이템/지형은 학생이 지정한 이름·색상으로 표시.
- `dijkstraFrontier` (TODO 6-1/6-2): 가중치 격자, 타일 클릭으로 가중치
  순환(1→2→3→5→9), 우선순위 큐 스냅샷, 음수 가중치 프리셋(오프셋
  정규화 시각화), "경로 비용 X (최적 Y) ✓/✗" 판정.
- `mapEditor` (TODO 7), `assetPicker` (TODO 8): **3차 작업에서 완전히
  구현했습니다.** 자세한 내용은 8절을 보세요.

**Play 탭**: 처음에는 일부러 고장난 상태로 시작해서, Required 단계를
**완료(스킵 아님)** 할 때마다 실제 기능이 하나씩 켜집니다. 우측 패널
"Play" 탭에서 항상 볼 수 있고, 켜진/꺼진 기능은 체크리스트로 보여줍니다.
10개 Required TODO를 모두 완료하면 "🏆 YOUR GAME IS LIVE" 배너가 뜹니다.
라운드 설정(3라운드의 rows/cols/아이템 수/제한시간 등)은 원본
`complete/settings.py`의 `ROUND_CONFIGS`·`ITEM_SCORE`·
`SWAMP_SCORE_PENALTY` 값을 그대로 JS 상수로 옮겨 썼습니다(파이썬 코드와는
독립적으로 동작). 사운드는 기본 꺼짐이며 학생이 직접 켜야만 재생됩니다
(자동재생 없음).

## 6. 프로젝트 상태 (4차 = 마지막 작업 완료)

이번 4차 작업으로 계획된 모든 기능이 구현되었습니다: 16개 TODO 채점 +
시각화 8종 + Play 탭 + 맵 에디터/에셋 피커 + 파이썬 프로젝트 export +
배포 패키징 + 교사용 문서. 남은 건 **실제 브라우저/학교 컴퓨터에서의
1회 점검**뿐입니다 — 9절의 첫 실행 체크리스트를 참고하세요.

## 7. 진행 상황 저장/이동

- 자동으로 `localStorage`에 저장됩니다 (코드 초안, 완료/스킵 상태, 힌트
  사용 개수, 시도 횟수, **맵 에디터로 그린 라운드**, **업로드한 파일
  목록(메타데이터만)** 전부).
- 여러 학생이 컴퓨터를 같이 쓰는 상황을 고려해서, 상단에 **"Save my work"**
  (progress.json 다운로드)와 **"Load my work"**(불러오기, 덮어쓰기 전 확인
  창 있음) 버튼을 눈에 잘 띄게 배치했습니다.
- 테마(라이트/다크)는 진행 상황과 별도로 저장되므로, "Reset all"을 눌러도
  테마 설정은 유지됩니다.
- **중요**: `progress.json`에는 업로드한 이미지/사운드 **파일 자체는
  들어가지 않습니다** (JSON에 넣기엔 부적합/용량 문제). 파일 목록(이름만)만
  기록되고, 실제 파일은 학생 디스크(또는 연결된 프로젝트 폴더)에 남아
  있어야 합니다. "Save my work" 버튼에 툴팁으로 안내했고, 업로드한 파일이
  있으면 저장할 때마다 확인 팝업으로 한 번 더 알려줍니다.

## 8. TODO 7 맵 에디터 / TODO 8 에셋 피커 (3차 작업 상세)

### TODO 7 — 맵 에디터
- 라운드마다 rows×cols를 직접 입력하고(탭 3개), 최소/최대 크기와 "고정
  게임 창 대비 타일이 너무 작아진다" 경고를 보여줍니다.
- **Generate**는 타일 하나하나를 무작위로 정하지 않고, 시드 포인트에서
  "랜덤워크 블롭"으로 자라나는 방식으로 벽/늪 뭉치를 만듭니다 (Cluster
  size 슬라이더로 조절). 생성 후 반드시 **연결성 복구(ensureConnectivity)**
  를 돌려서 모든 칸이 도달 가능하도록 보장합니다 — 이 알고리즘은
  브라우저가 없는 이 작업 환경에서 파이썬으로 그대로 포팅해서 5가지
  크기/시드 조합 전부 "완전 연결 + 목표/보물 전부 도달 가능"을
  확인했습니다.
- 지형 종류는 원본 그대로: WALL(벽)/NORMAL(바닥)/SWAMP(늪)/TREASURE(보물)/
  BOMB(폭탄)/GOAL/START, 그리고 **TODO 10을 완료한 경우에만** 커스텀
  지형이 팔레트에 추가됩니다(새 바이옴 없음).
- 클릭 페인트(드래그 가능), 브러시 1/2/3, 실행취소/다시실행 20단계,
  도달 불가 타일을 어둡게 표시하는 실시간 검증 + "Goal reachable ✓ · 2
  treasures unreachable ✗" 같은 판정 문구.
- **코드 양방향 동기화**: 그림을 그리면 TODO 7 에디터의 ROUND_CONFIGS
  숫자(rows/cols/cell_size/swamp_count/item_count/bomb_count/
  extra_open_walls/time_limit_seconds)가 자동으로 갱신됩니다. 반대로
  코드를 손으로 고치면 크기 입력칸이 따라 바뀌고, 그림과 숫자가
  서로 다르면 조용히 덮어쓰지 않고 "Use my code / Use the painted map"
  버튼으로 학생이 직접 고르게 합니다. 채점은 여전히 1차 작업의
  syntax 검증기가 최종 권위자입니다.
- 키보드로도 전부 가능: 캔버스를 클릭(포커스)한 뒤 방향키로 커서 이동,
  Enter로 페인트.
- TODO 7을 완료하면 Play 탭이 해당 라운드에서 **절차적 생성 대신 학생이
  그린 지도를 그대로 사용**합니다(DFS 생성 호출 자체를 건너뜁니다).

### TODO 8 — 에셋 피커
- 번들 이미지(썸네일 그리드)/사운드(재생 버튼 목록) 클릭 한 번으로 올바른
  변수(`PLAYER_IMAGE_PATH` 등 10개)에 경로가 채워지고, 실제 타일 위
  미리보기와 사운드 미리듣기를 제공합니다.
- **직접 업로드**: 우선 File System Access API로 `dijkstra_maze/student`
  폴더에 연결하면(권한 대화상자 안내 포함) 파일이 `assets/images` 또는
  `assets/sounds`에 바로 저장되고 경로가 자동 입력됩니다. 폴더 핸들은
  IndexedDB에 저장해서 다음 방문 때 재연결 버튼 한 번이면 됩니다.
- API 미지원/권한 거부/`file://` 환경에서는 자동으로 파일명을 안전하게
  바꿔서 다운로드를 준비하고, 정확히 어디에 넣어야 하는지 안내합니다
  (file:// 감지 시 `로컬서버_실행.bat` 안내 포함).
- 업로드 시 검증: 확장자 확인, 큰 이미지 자동 축소 제안(128×128), 사운드는
  WAV 권장(다른 포맷은 경고), 파일명 새니타이즈(한글/공백 → 안전한
  영문/언더스코어, 학생에게 새 이름 안내), 번들 자산과 이름 겹치면 자동
  접미사, 이번 세션에 추가한 파일 목록 + 제거 버튼.
- 안내문은 패널에 항상 보이는 번호 매긴 단계별 설명(영어) + 폴더 구조를
  보여주는 작은 인라인 SVG 아이콘으로 구성했습니다.

### 알아두면 좋은 점 (다음 작업 전에)
- 맵 에디터는 **타일 기반(칸 자체가 벽/바닥)** 모델을 씁니다. 기존
  DFS 절차적 생성은 **간선 기반(칸 사이 벽)** 모델을 씁니다. 서로 다른
  표현이지만 `paintedGridToWallGrid()`가 타일 모델을 기존 간선-벽
  포맷으로 변환해 주기 때문에, Play 탭이나 트레이스 하네스 등 기존
  코드는 전혀 손대지 않았습니다.
- ROUND_CONFIGS 코드↔그림 동기화 파서는 "베스트 에포트"입니다 (정규식
  기반, 학생이 아주 특이한 포맷으로 다시 쓰면 못 읽을 수 있음) — 그럴 때는
  조용히 실패하지 않고 "코드를 읽을 수 없어서 마지막 그린 지도를
  보여줍니다"라고 안내만 하고, **채점 자체는 여전히 1차 작업의 정식
  syntax 검증기**가 담당하므로 안전합니다.
- File System Access API 관련 코드(폴더 연결/쓰기/IndexedDB)는 이 작업
  환경에 브라우저가 없어 **실제 Chrome/Edge에서 아직 테스트하지
  못했습니다.** 권한 대화상자 흐름, 재연결, 다운로드 폴백 세 갈래를
  실제 브라우저에서 한 번씩 눌러보는 걸 권장합니다.
- 커스텀 업로드 파일의 "삭제" 버튼은 진행 목록에서만 제거하며, 폴더가
  연결돼 있으면 실제 파일 삭제도 시도하지만 실패해도 조용히 넘어갑니다
  (권한/브라우저 지원 문제일 수 있음).

## 9. "Download my project" — 파이썬 프로젝트 export (4차 작업 상세)

상단 바의 **"Download my project"** 버튼을 누르면 나오는 모달에서 세 가지를
고를 수 있습니다.

### 동작 원리
- `export-data.js`에 `student/`의 원본 스타터 파일 5개(TODO가 있는 파일:
  settings.py/game.py/player.py/maze.py/pathfinding.py)를 **한 글자도 안
  바꾸고** 그대로 담아뒀고, TODO 마다 시작/끝 마커 텍스트와 원래 들여쓰기
  깊이만 별도로 기록해 뒀습니다 (`todos.json`의 `begin_marker`/`end_marker`/
  `indent` 필드 그대로 — 정답 필드는 가져오지 않았습니다).
- 각 TODO에 대해: **완료(completed)** 상태면 학생이 실제로 작성해서 통과한
  코드를, **그 외(스킵/미완료)**면 원래 스타터 코드(`pass` 등)를 원래
  들여쓰기에 맞춰 마커 사이에 그대로 끼워 넣습니다. 마커 바깥 코드는
  단 한 글자도 건드리지 않습니다.
- 끼워 넣기 전, 각 TODO를 **하나씩 누적 적용**하면서 그때그때
  `ast.parse`(Pyodide)로 문법을 확인합니다. 만약 특정 TODO를 넣는 순간
  파일이 깨지면, 정확히 그 TODO 번호를 지목하고 **그 파일은 안전하게
  스타터 버전으로 대체**해서 내보냅니다(게임이 실행 안 되는 최악의 상황을
  방지). 완료/미완료 요약도 모달에 "8 of 10 Required complete · 2 skipped
  · Bonus 1 of 6" 형태로 보여줍니다.
- **미완료 TODO가 있으면** 해당 파일 맨 위에 "이 TODO들은 아직
  끝나지 않았습니다"라는 주석 블록이 자동으로 추가됩니다. 정답을 몰래
  채워 넣는 일은 절대 없습니다.

### 세 가지 내보내기 방법
1. **전체 zip 다운로드** — 압축 없는(store-only) 순수 JS ZIP 라이터로
   settings.py~pathfinding.py(스플라이스 완료) + cell.py/goal.py/items.py/
   main.py/requirements.txt(원본 그대로) + assets/images, assets/sounds
   (사이트에 이미 있는 번들 자산을 그 자리에서 fetch해서 zip에 포함) +
   `HOW_TO_RUN.txt`를 담아 다운로드합니다. 압축을 안 쓰는 대신(store
   method), 외부 라이브러리 없이 순수 JS로 구현했고, Python
   `zipfile`로 실제 열어서 CRC까지 확인하는 방식으로 포맷을 검증했습니다.
2. **파일 하나만 다운로드** — settings.py/game.py/player.py/maze.py/
   pathfinding.py 중 하나를 골라 그 파일만 받습니다 (이미 폴더가 있고
   하나만 갱신하고 싶을 때).
3. **연결된 프로젝트 폴더에 바로 쓰기** — TODO 8에서 폴더를 연결해 둔
   경우에만 나타납니다. 어떤 파일이 바뀌는지 미리 보여주고, 확인 버튼을
   눌러야 실행되며, 덮어쓰기 전 각 원본을 `<파일명>.py.bak`으로 자동
   백업합니다. 파일별 성공/실패를 따로 보고합니다.

### 검증 방법 (참고용)
Node.js/브라우저가 없는 이 작업 환경에서, 스플라이싱·재들여쓰기 로직을
파이썬으로 그대로 포팅해서 확인했습니다: (1) 실제 정답으로 채운 5개
파일이 전부 `ast.parse` 통과, (2) 마커 바깥 줄이 원본과 100% 동일,
(3) 아무것도 완료 안 한 "새 학생" 시나리오에서는 출력이 원본 스타터
파일과 완전히 동일(바이트 단위)함을 확인. ZIP 포맷도 Python `zipfile`로
직접 열어 CRC 검증까지 통과했습니다. **다만 실제 브라우저에서 내려받은
zip을 풀어서 `python main.py`로 진짜 실행해보는 것은 아직 못 했습니다** —
9절 체크리스트의 최우선 항목입니다.

## 10. assets 폴더가 두 곳에 있는 이유

`dijkstra_maze_web/assets/`(이 웹사이트용)와 학생이 내려받는 파이썬
프로젝트 안의 `assets/`(zip에 포함되거나 연결된 폴더에 이미 있는 것)는
**같은 파일이지만 서로 다른 두 개의 사본**입니다. 웹사이트는 정적
파일만 서빙할 수 있어서 자바스크립트가 파이썬 프로젝트 폴더를 직접
참조할 수 없기 때문에, 시각화/Play 탭/에셋 피커 미리보기용으로 한 벌을
`dijkstra_maze_web/assets/`에 복사해 뒀습니다. zip으로 내보낼 때는 이
사본을 다시 zip 안에 넣어주므로 학생이 특별히 신경 쓸 필요는 없지만,
**TODO 8에서 학생이 직접 업로드한 커스텀 이미지/사운드는 이 웹사이트
사본에는 없고 학생 컴퓨터(또는 연결된 프로젝트 폴더)에만 있습니다** —
그래서 zip에는 자동으로 포함되지 않고, `HOW_TO_RUN.txt`와 export 모달에서
"직접 복사해 넣으세요"라고 안내합니다.

## 11. 학교 컴퓨터 첫 실행 체크리스트

실제로 배포하기 전에 학교 컴퓨터(또는 아무 컴퓨터) 한 대에서 아래를 한 번씩
직접 눌러보시길 권합니다 — 전부 이 작업 환경(브라우저 없음)에서는 실행해볼
수 없었던 항목들입니다.

1. **Pyodide 로딩**: 아무 TODO에서나 Grade를 처음 눌렀을 때 로딩 배너가
   뜨고, 잠시 후 정상적으로 채점 결과가 나오는지.
2. **오답이 멈추지 않고 애니메이션되는지**: 예를 들어 TODO 3-3을 일부러
   `pass`로 두고 dfsGenerate 시각화의 "Run my code"를 눌렀을 때, 무한
   루프에 빠지지 않고 "Stopped after 2000 steps" 같은 메시지와 함께
   멈추는지.
3. **폴더 연결(Connect my project folder)**: TODO 8에서 실제
   `dijkstra_maze/student` 폴더를 선택하고 권한을 허용했을 때 정상
   연결되는지, 페이지를 새로고침한 뒤 "Reconnect" 한 번으로 재연결되는지.
4. **업로드가 올바른 위치에 들어가는지**: 이미지 하나를 업로드했을 때
   실제로 `assets/images/`에 파일이 생기고, settings.py 코드에 올바른
   경로 문자열이 채워지는지.
5. **내보낸 zip이 진짜 실행되는지**: "Download my project"로 zip을 받아
   압축을 풀고, `pip install -r requirements.txt` 후 `python main.py`로
   실제 게임이 뜨는지 — 이것이 **가장 중요한 최종 확인**입니다.
6. **진행 상황 export/import 왕복**: "Save my work"로 progress.json을
   받은 뒤 "Reset all"로 초기화하고, 그 progress.json을 "Load my work"로
   불러와서 모든 코드/완료 상태/맵/업로드 목록이 그대로 복원되는지.
7. **내보낸 스플라이스 파일이 실제로 학생이 쓴 코드와 일치하는지**: 아무
   TODO나 하나 골라 코드 에디터에 적은 내용과, 내보낸 zip 안 해당 파일의
   마커 사이 내용을 비교해서 재들여쓰기만 되고 내용은 그대로인지 확인.

## 12. 교사용 자료

**위치: `c:/Users/손채영/Desktop/kazh/pygame/교사용/`** (이 `dijkstra_maze_web/`
폴더 **바깥**, `dijkstra_maze`/`dijkstra_maze_web`과 같은 레벨의 형제
폴더입니다 — `로컬서버_실행.bat`이 서빙하는 범위 밖이라 학생에게 노출될
위험이 없습니다). 한국어로 세 파일을 준비했습니다:
- `정답_해설.md` — 16개 TODO 전체 정답 해설(개념, 자주 하는 실수, 채점기가
  허용하는 다른 정답, 힌트 3단계).
- `수업_운영안.md` — 단계별 예상 소요 시간, 막히기 쉬운 지점, 시연 포인트,
  Skip 활용법, 제출물 수거 방법, 채점 기준안.
- `체크리스트.md` — `student/TODO_CHECKLIST.md`와 같은 형식의 16개 TODO
  한 페이지 표.

**학생에게는 `dijkstra_maze_web/` 폴더만 전달하세요** (`교사용/`은 별도
보관).
