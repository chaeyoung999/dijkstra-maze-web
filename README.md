# Dijkstra Maze — TODO Quest (웹 버전) 안내

학생들이 `student/` 폴더 6개 파일을 직접 뒤져서 TODO를 찾는 대신, 한 페이지짜리
웹사이트에서 15개 TODO(Required 7 + Bonus 8)를 한 단계씩 순서대로 풀 수 있도록
만든 결과물입니다.

**2차 작업**에서 시각화 8종 + Play 탭 게임을, **3차 작업**에서 맵 에디터와
에셋 업로드를 완전히 구현했습니다. 학생이 상단의 "Run my code"를
누르거나(또는 각 시각화 패널의 "↻ Replay animation" 버튼을 눌러서 오답 상태에서도)
실제로 자기 코드를 Pyodide로 실행해서 나온 결과를 애니메이션으로 보여줍니다 —
미리 만들어둔 "정답 애니메이션"이 아니라, 진짜 자기 코드가 만든 결과입니다.

> **📌 5차 작업**: 실제 수업 파일럿 결과(목표 1시간 대비 약 2시간 소요)에
> 따라 (1) 기존 TODO 3-1/3-2/3-3(`maze.py`, DFS 미로 생성)을 **완전히
> 제거**, (2) 나머지 모든 TODO의 설명을 학생들이 먼저 들은 "Game AI Lab:
> FSM × Dijkstra" 수업과 명시적으로 연결, (3) 문서/타이밍을 갱신했습니다.

> **📌 6차 작업(이번 업데이트, 지금까지 가장 큰 콘텐츠 변경)**: 실제 수업
> 파일럿 이후 교사 피드백을 반영해 세 가지를 바꿨습니다.
> 1. **Required BFS 단계를 실제 Dijkstra로 교체**(구 Bonus 6-1/6-2 흡수):
>    학생들이 직전 수업(Game AI Lab 미션 8)에서 이미 배운 바로 그 완화
>    (relaxation) 공식이므로, BFS만 가르치고 나중에 "진짜" 알고리즘을
>    보너스로 또 가르치는 건 순서가 거꾸로였습니다. 이제 Required TODO
>    5 하나가 `new_cost` 계산 + `distance`/`parent`/`queue` 갱신을 전부
>    담당하고, 가중치 없이 호출하면 그대로 BFS가 되는 성질을 이용해
>    늪 배치·힌트 버튼·(신규) 몬스터 추격까지 세 가지 역할을 동시에
>    합니다. 이로써 구 Bonus 6-1/6-2는 완전히 중복이 되어 제거했습니다.
> 2. **TODO 1을 둘로 분리**: Required "게임 이름 짓기"(TITLE/GAME_SUBTITLE)
>    + 새 캡스톤 Bonus "게임 규칙 쓰기"(MISSION_RULES/HOW_TO_PLAY_RULES,
>    이제 TODO 15). 규칙은 게임이 실제로 완성된 뒤에 써야 의미가 있으므로,
>    **다른 모든 Bonus를 완료(또는 건너뛰기)해야만 잠금 해제**되는 이번
>    세션의 유일한 예외 규칙이 적용됩니다.
> 3. **신규 몬스터 Bonus 기능**: FSM(PATROL/CHASE/ATTACK) 상태 머신을 가진
>    추격 몬스터(`monster.py`, 신규 파일)를 추가했습니다 — TODO 12(튜닝
>    상수)/13(상태 전환)/14(추격 이동, Required TODO 5의 다익스트라 함수
>    재사용). 아이템도 `CUSTOM_ITEM_*` 한 개에서 `CUSTOM_ITEMS` 리스트로
>    확장(TODO 10)했고, 맵 에디터에서 몬스터 시작 위치도 그릴 수 있습니다.
> 4. **TODO 번호를 전부 1..15 정수로 재정렬**: `2-1/2-2/2-3`,
>    `5-1/5-2`, `6-1/6-2` 같은 하이픈 번호 체계를 완전히 없앴습니다.
>
> 자세한 배경은 `../dijkstra_maze/TEACHER_TODO_GUIDE.md`와
> `교사용/수업_운영안.md` 참고.

**4차 작업에서 세 가지를 추가했습니다:**
1. **"Download my project"** — 학생의 답안을 실제로 실행되는 파이썬
   프로젝트로 되돌려주는 기능 (zip 다운로드 / 파일 하나만 다운로드 /
   연결된 폴더에 바로 쓰기).
2. **배포 패키징 점검** — 이 폴더 하나만으로 완결되는지 최종 확인.
3. **`교사용/` 폴더** — 정답 해설, 수업 운영안, 체크리스트 (한국어).
   **`dijkstra_maze_web/` 바깥, `c:/Users/손채영/Desktop/kazh/pygame/교사용/`
   에 따로 만들었습니다** — `로컬서버_실행.bat`이 `dijkstra_maze_web/`
   폴더만 서빙하므로, 이 폴더 안에 있으면 학생이 브라우저로 그 주소를
   추측해서 열어볼 위험이 있기 때문입니다. 학생에게는 `dijkstra_maze_web/`
   폴더만 배포하세요.

## 1. 실행 방법

1. `dijkstra_maze_web` 폴더를 통째로 학생에게 배포합니다.
2. **`로컬서버_실행.bat`을 더블클릭**하면:
   - PC에 설치된 파이썬으로 `python -m http.server 8000`을 실행하고
   - 자동으로 브라우저에서 `http://localhost:8000`을 엽니다.
   - 창을 닫으면 서버가 꺼지므로, 작업하는 동안 창을 열어두라고 안내됩니다.
3. 파이썬이 PC에 없으면 배치 파일이 설치 안내 메시지를 띄웁니다
   (python.org에서 설치 시 "Add python.exe to PATH" 체크 필요).
4. `index.html`을 그냥 더블클릭해서 `file://`로 열어도 페이지는 뜨지만,
   상단에 "로컬 서버로 다시 실행해 달라"는 경고 배너가 뜹니다. 채점 자체는
   Pyodide(CDN)를 쓰기 때문에 file://에서도 동작은 하지만, 이후 작업(파일
   업로드 등 File System Access API)은 반드시 `http://localhost`가 필요하므로
   지금부터 로컬 서버 사용을 권장/습관화하는 것이 좋습니다.
5. 인터넷 연결이 필요합니다 (Pyodide를 CDN에서 최초 1회 내려받음, 이후에는
   브라우저 캐시를 씀).

## 2. 파일 구성

| 파일 | 역할 |
|---|---|
| `index.html` | 페이지 뼈대(HTML). 실제 내용은 전부 `app.js`가 그려 넣습니다. |
| `styles.css` | 전체 디자인, 라이트/다크 테마, 반응형 레이아웃 + 시각화/게임 UI + 맵 에디터·에셋 피커 UI + export 모달 스타일 + 캡스톤 잠금 배너 스타일(6차 추가). |
| `data.js` | 15개 TODO의 **콘텐츠**만 담은 순수 데이터. `REQUIRED_ORDER`/`BONUS_ORDER`/`CAPSTONE_BONUS_ID`도 여기서 내보냅니다. |
| `export-data.js` | `student/` 원본 6개 파일(TODO 있는 파일 + 신규 `monster.py`)의 **스타터 상태 그대로**를 담은 콘텐츠 전용 데이터 + 각 TODO의 마커 위치(파일/들여쓰기/시작-끝 마커)만 담고 있습니다. 정답은 전혀 없습니다 — Python export 기능의 "붙여넣기 베이스"입니다. |
| `app.js` | 실제 동작 전부: 단계 진행 로직(**+ 이번 6차 작업에서 추가한 캡스톤 잠금 규칙**), 코드 에디터, Pyodide 로딩, 채점 엔진, 진행 상황 저장/불러오기, 시각화, Play 탭 게임 엔진(**+ 몬스터 배치/렌더링**), `mapEditor`/`assetPicker`, 프로젝트 export, ast.parse 검증, HOW_TO_RUN.txt 생성기. |
| `assets/images/`, `assets/sounds/` | `student/assets`에서 그대로 복사한 스프라이트/효과음. |
| `로컬서버_실행.bat` | 로컬 서버 실행 + 브라우저 자동 오픈. |
| `README.md` | 이 문서 (교사용, 학생에게는 배포할 필요 없음). |

> **`교사용/` 폴더는 이 폴더(`dijkstra_maze_web/`) 안에 없습니다.** 학생에게
> 배포하는 폴더와 절대 섞이지 않도록 `dijkstra_maze_web/`의 **바깥**,
> `c:/Users/손채영/Desktop/kazh/pygame/교사용/`에 따로 있습니다. 12절 참고.

## 3. 채점은 어떻게 동작하나요?

- 브라우저에서 [Pyodide](https://pyodide.org)(파이썬을 브라우저 안에서 돌리는
  런타임, WebAssembly 기반)를 CDN에서 불러옵니다. 첫 채점 시도 시 로딩하며,
  로딩 중에는 안내 배너와 버튼 비활성화로 표시됩니다. 로딩 실패 시 채점
  버튼이 비활성화되고 에러 배너가 뜹니다 (조용히 넘어가지 않음).
- **behaviour 모드 (8개 TODO: 2, 3, 4, 5, 6, 7, 13, 14)**: 학생 코드를
  그대로 가져다가, 격리된 미니 테스트 환경(가짜 Cell/Maze/Player/Monster
  등)에 끼워 넣고 **행동(behaviour)만** 비교합니다. 변수 이름이 다르거나
  코드 구조가 달라도 "행동이 같으면" 통과합니다(요구사항의 핵심). TODO 5
  (Dijkstra 완화)는 신선/개선/비개선/음수 가중치 4가지 경우를 모두 확인하고,
  TODO 14(몬스터 추격)는 학생이 실제로 작성한 TODO 5 코드에 의존하지 않는
  독립적인 정답 다익스트라 구현으로 채점합니다. 재귀/루프 관련 하네스는
  하드 캡을 걸어서, 학생 코드가 잘못돼도 브라우저가 멈추지 않도록 했습니다.
- **syntax 모드 (7개 TODO: 1, 8, 9, 10, 11, 12, 15)**: 정답이 하나가 아닌
  자유 설계 문제라서, `ast.parse`로 문법 확인 + 실행 시 에러가 없고 필수
  변수명이 전부 정의됐는지만 완료 처리 기준으로 삼습니다. 타입/범위/딕셔너리
  키 보존 등은 비차단 "참고" 메시지로만 안내합니다. TODO 15는 추가로
  **캡스톤 잠금**이 걸려 있어서, 채점 결과와 별개로 다른 모든 Bonus가
  완료/건너뛰기 되기 전까지는 애초에 열리지 않습니다.
- 에러 메시지는 항상 "무엇이 맞았고 무엇이 틀렸는지"를 구체적으로 알려주도록
  작성했습니다 (`✓ ... / ✗ ...` 형태). 파이썬 문법 오류(SyntaxError 등)는
  로직 검사보다 먼저, 줄 번호와 함께 표시됩니다.
- **채점 하네스(테스트 코드)는 정답을 담고 있지 않습니다.** 정답과 비교하는
  게 아니라, 몇 가지 입력값에 대해 기대되는 "결과"만 확인합니다. 예를 들어
  TODO 6은 "점수가 0에서 시작해서 n개 모으면 n×ITEM_SCORE가 되는가"만
  확인하지, `self.score += ITEM_SCORE`라는 코드 자체를 담고 있지 않습니다.

## 4. 이번 6차 작업 검증 방법 (참고용)

이 작업 환경에는 Node.js나 실제 브라우저가 없어서, 대신 다음과 같이
검증했습니다:
- 실제 pygame이 설치되어 있어서, `complete/`와 `student/`(완전 미완성 상태)
  게임을 **headless(SDL dummy driver)로 진짜 실행**하고: 몬스터 스폰,
  PATROL→CHASE→ATTACK 상태 전환의 정확한 거리 경계, 몬스터에게 잡혔을 때
  플레이어 위치 리셋 + 점수 페널티, 그리고 몬스터의 추격 이동이 실제로
  플레이어 쪽으로 매 프레임 정확히 한 칸씩 가까워지는지를 직접 확인했습니다.
- `app.js`가 만들어내는 **모든 채점용 파이썬 코드**(behaviour + syntax
  하네스, 신규 `dijkstra_5`/`monsterFsm_13`/`monsterChase_14` 포함)를 실제
  파일에서 그대로 추출해 실행해 보고, 정답/오답 예시 각각에 대해 pass/fail이
  올바르게 나오는지 15개 TODO 전부 확인했습니다.
- JS 파일 전체(`app.js`/`data.js`/`export-data.js`)의 괄호/따옴표 짝이
  맞는지 별도 스크립트로 확인했습니다.
- **정답 유출 감사**: `data.js`의 lead/hint 텍스트에 채점 정답 문자열이
  그대로 노출되지 않는지 자동으로 대조했습니다 — 이 과정에서 신규 TODO
  13/14 힌트 3단계가 실제 정답을 그대로 노출하고 있던 것을 발견해
  `???` 빈칸 스타일로 수정했습니다 (기존 TODO들과 동일한 컨벤션).
- **캡스톤 잠금 로직**: 실제 `computeStatus`/`otherBonusAllDone`/
  `nextStepAfter` 로직을 그대로 추출해서, "다른 Bonus 0개 완료(잠김) →
  일부만 완료(여전히 잠김) → 전부 완료/건너뛰기(잠금 해제)"의 세 시나리오를
  전부 통과하는지 확인했습니다.
- 그래도 실제 브라우저(Chrome 등)에서 Pyodide가 정상적으로 로드되고,
  에디터 자동완성/Tab/Ctrl+Enter, 캔버스 애니메이션, Play 탭 조작감,
  File System Access API 권한 흐름/IndexedDB 재연결/드래그앤드롭 업로드
  등 UI 동작이 매끄러운지는 **아직 실제 브라우저로 직접 열어서 확인해보지
  않았습니다.** 11절의 첫 실행 체크리스트를 참고하세요.

## 5. 시각화 7종 + Play 탭

> **📌 변경(6차)**: `bfsFlood` 시각화는 제거되고 `dijkstraFrontier`가 그
> 역할까지 흡수했습니다(Required TODO 5가 BFS 역할까지 겸하므로, 별도
> 시각화를 유지할 필요가 없어졌습니다). 대신 신규 `monsterLab`이
> 추가됐지만, 시간 관계상 실제 캔버스 애니메이션은 아직 구현하지 않고
> "이 패널은 코드가 완성되면 애니메이션됩니다"라는 플레이스홀더로
> 대체했습니다(기존에 이미 있던 "미등록 시각화 자동 대체" 장치를 그대로
> 활용) — 결과적으로 시각화 종류 수는 7종으로 그대로입니다.

**핵심 원칙: 애니메이션을 절대 가짜로 만들지 않습니다.** 모든 시각화와 Play
탭의 게임 로직(이동, 다익스트라, 점수 계산, 힌트 경로)은 학생이 **지금
저장한 실제 코드**를 Pyodide 하네스에 끼워 실행하고, 그 결과를 JSON
"trace"(이벤트 목록)로 받아 캔버스에 애니메이션으로 재생합니다. 오답이면
진짜 오답 결과가 나옵니다 (예: TODO 3이 틀리면 벽을 그냥 통과, TODO 4가
틀리면 경로가 비어 있음 등).

- `titleCard` (TODO 1, TODO 15): 학생의 TITLE/GAME_SUBTITLE를 실시간
  미리보기(TODO 1), MISSION_RULES/HOW_TO_PLAY_RULES를 실시간 미리보기
  (TODO 15, 캡스톤).
- `playerMove` (TODO 2/3/4): 고정된 작은 미로 위에서 캔버스를 클릭한
  뒤 방향키/WASD로 움직이면 실제 try_move 로직이 실행됩니다. 벽 통과 횟수,
  마지막 방향, moved/try_move 반환값을 실시간 표시.
- `dijkstraFrontier` (TODO 5): 가중치 격자, 타일 클릭으로 가중치 순환,
  우선순위 큐 스냅샷, 음수 가중치 프리셋(오프셋 정규화 시각화), "경로
  비용 X (최적 Y) ✓/✗" 판정 + 늪 배치(균등 가중치 데모) 패널을 한
  시각화가 모두 담당합니다(구 bfsFlood + dijkstraFrontier 통합).
- `scoreBoard` (TODO 6/7/10/11): 타일을 하나씩 재생하며 실제 점수 변화
  확인. 커스텀 아이템(이제 리스트 중 첫 번째)/지형은 학생이 지정한
  이름·색상으로 표시.
- `mapEditor` (TODO 8), `assetPicker` (TODO 9): 3절 참고. 맵 에디터는
  이번 세션부터 몬스터 시작 위치도 그릴 수 있습니다.
- `monsterLab` (TODO 12/13/14): 현재는 플레이스홀더 패널입니다(위 참고).

**Play 탭**: 처음에는 일부러 고장난 상태로 시작해서, Required 단계를
**완료(스킵 아님)** 할 때마다 실제 기능이 하나씩 켜집니다. 우측 패널
"Play" 탭에서 항상 볼 수 있고, 켜진/꺼진 기능은 체크리스트로 보여줍니다.
7개 Required TODO를 모두 완료하면 "🏆 YOUR GAME IS LIVE" 배너가 뜹니다.
몬스터(Bonus)는 라운드마다 항상 스폰되며(실제 게임의 Monster 클래스가
TODO 완료 여부와 무관하게 항상 존재하는 것과 동일), 이 브라우저 미니
프리뷰에서는 시간 관계상 실시간 Pyodide 기반 FSM/추격 시뮬레이션 대신
**단순 JS 랜덤워크(순찰)**로 움직이고, 플레이어와 같은 칸에 있으면
점수 페널티 + 시작 위치로 리셋됩니다(실제 게임의 given PATROL 동작과
동일한 수준의 단순화 — 공개된 축소 범위입니다). 라운드 설정은 원본
`complete/settings.py`의 `ROUND_CONFIGS` 값을 그대로 JS 상수로 옮겨
썼습니다. 사운드는 기본 꺼짐이며 학생이 직접 켜야만 재생됩니다.

## 6. 프로젝트 상태

계획된 모든 기능이 구현되어 있습니다: 15개 TODO 채점 + 시각화 7종 + Play 탭
+ 맵 에디터/에셋 피커 + 캡스톤 잠금 + 파이썬 프로젝트 export + 배포 패키징
+ 교사용 문서. 남은 건 **실제 브라우저/학교 컴퓨터에서의 1회 점검**뿐입니다
— 11절의 첫 실행 체크리스트를 참고하세요.

## 7. 진행 상황 저장/이동

- 자동으로 `localStorage`에 저장됩니다 (코드 초안, 완료/스킵 상태, 힌트
  사용 개수, 시도 횟수, 맵 에디터로 그린 라운드, 업로드한 파일
  목록(메타데이터만) 전부).
- 여러 학생이 컴퓨터를 같이 쓰는 상황을 고려해서, 상단에 **"Save my work"**
  (progress.json 다운로드)와 **"Load my work"**(불러오기, 덮어쓰기 전 확인
  창 있음) 버튼을 눈에 잘 띄게 배치했습니다.
- 테마(라이트/다크)는 진행 상황과 별도로 저장되므로, "Reset all"을 눌러도
  테마 설정은 유지됩니다.
- **중요**: `progress.json`에는 업로드한 이미지/사운드 **파일 자체는
  들어가지 않습니다**. 파일 목록(이름만)만 기록되고, 실제 파일은 학생
  디스크(또는 연결된 프로젝트 폴더)에 남아 있어야 합니다.

## 8. TODO 8 맵 에디터 / TODO 9 에셋 피커

### TODO 8 — 맵 에디터
- 라운드마다 rows×cols를 직접 입력하고(탭 3개), 최소/최대 크기와 "고정
  게임 창 대비 타일이 너무 작아진다" 경고를 보여줍니다.
- **Generate**는 타일 하나하나를 무작위로 정하지 않고, 시드 포인트에서
  "랜덤워크 블롭"으로 자라나는 방식으로 벽/늪 뭉치를 만듭니다. 생성 후
  반드시 **연결성 복구(ensureConnectivity)**를 돌려서 모든 칸이 도달
  가능하도록 보장합니다.
- 지형 종류는: WALL(벽)/NORMAL(바닥)/SWAMP(늪)/TREASURE(보물)/BOMB(폭탄)/
  **MONSTER(몬스터 시작 위치, 신규)**/GOAL/START, 그리고 **TODO 11을
  완료한 경우에만** 커스텀 지형이 팔레트에 추가됩니다.
- 클릭 페인트(드래그 가능), 브러시 1/2/3, 실행취소/다시실행 20단계,
  도달 불가 타일을 어둡게 표시하는 실시간 검증 + "Goal reachable ✓ · 2
  treasures unreachable ✗" 같은 판정 문구.
- **코드 양방향 동기화**: 그림을 그리면 TODO 8 에디터의 ROUND_CONFIGS
  숫자(rows/cols/cell_size/swamp_count/item_count/bomb_count/
  **monster_count(신규)**/extra_open_walls/time_limit_seconds)가 자동으로
  갱신됩니다. 반대로 코드를 손으로 고치면 크기 입력칸이 따라 바뀌고,
  그림과 숫자가 서로 다르면 조용히 덮어쓰지 않고 "Use my code / Use the
  painted map" 버튼으로 학생이 직접 고르게 합니다.
- 키보드로도 전부 가능: 캔버스를 클릭(포커스)한 뒤 방향키로 커서 이동,
  Enter로 페인트.
- TODO 8을 완료하면 Play 탭이 해당 라운드에서 **절차적 생성 대신 학생이
  그린 지도를 그대로 사용**합니다.

### TODO 9 — 에셋 피커
- 번들 이미지(썸네일 그리드)/사운드(재생 버튼 목록) 클릭 한 번으로 올바른
  변수(`PLAYER_IMAGE_PATH` 등 11개, **신규 `MONSTER_IMAGE_PATH` 포함**)에
  경로가 채워지고, 실제 타일 위 미리보기와 사운드 미리듣기를 제공합니다.
- **직접 업로드**: File System Access API로 `dijkstra_maze/student` 폴더에
  연결하면 파일이 `assets/images` 또는 `assets/sounds`에 바로 저장되고
  경로가 자동 입력됩니다. 폴더 핸들은 IndexedDB에 저장해서 다음 방문 때
  재연결 버튼 한 번이면 됩니다.
- API 미지원/권한 거부/`file://` 환경에서는 자동으로 파일명을 안전하게
  바꿔서 다운로드를 준비하고, 정확히 어디에 넣어야 하는지 안내합니다.

### 알아두면 좋은 점
- 맵 에디터는 **타일 기반** 모델을, 기존 DFS 절차적 생성은 **간선 기반**
  모델을 씁니다. `paintedGridToWallGrid()`가 변환해 주므로 Play 탭이나
  트레이스 하네스 등 기존 코드는 손대지 않았습니다.
- File System Access API 관련 코드(폴더 연결/쓰기/IndexedDB)는 이 작업
  환경에 브라우저가 없어 **실제 Chrome/Edge에서 아직 테스트하지
  못했습니다.**

## 9. "Download my project" — 파이썬 프로젝트 export

상단 바의 **"Download my project"** 버튼을 누르면 나오는 모달에서 세 가지를
고를 수 있습니다.

### 동작 원리
- `export-data.js`에 `student/`의 원본 스타터 파일 6개(TODO가 있는 파일:
  settings.py/game.py/player.py/pathfinding.py/**monster.py(신규)** +
  maze.py)를 **한 글자도 안 바꾸고** 그대로 담아뒀고, TODO 마다 시작/끝
  마커 텍스트와 원래 들여쓰기 깊이만 별도로 기록해 뒀습니다
  (`todos.json`의 `begin_marker`/`end_marker`/`indent` 필드 그대로 — 정답
  필드는 가져오지 않았습니다).
- 각 TODO에 대해: **완료(completed)** 상태면 학생이 실제로 작성해서 통과한
  코드를, **그 외(스킵/미완료)**면 원래 스타터 코드를 원래 들여쓰기에 맞춰
  마커 사이에 그대로 끼워 넣습니다.
- 끼워 넣기 전, 각 TODO를 **하나씩 누적 적용**하면서 그때그때
  `ast.parse`(Pyodide)로 문법을 확인합니다(이제 monster.py도 검증
  대상입니다). 만약 특정 TODO를 넣는 순간 파일이 깨지면, 정확히 그 TODO
  번호를 지목하고 **그 파일은 안전하게 스타터 버전으로 대체**해서
  내보냅니다.

### 세 가지 내보내기 방법
1. **전체 zip 다운로드** — settings.py~monster.py(스플라이스 완료) +
   cell.py/goal.py/items.py/main.py/maze.py/requirements.txt(원본 그대로) +
   assets/images, assets/sounds + `HOW_TO_RUN.txt`를 담아 다운로드합니다.
2. **파일 하나만 다운로드** — 6개 파일 중 하나만 받습니다.
3. **연결된 프로젝트 폴더에 바로 쓰기** — TODO 9에서 폴더를 연결해 둔
   경우에만 나타납니다. 덮어쓰기 전 각 원본을 `<파일명>.py.bak`으로 자동
   백업합니다.

## 10. assets 폴더가 두 곳에 있는 이유

`dijkstra_maze_web/assets/`(이 웹사이트용)와 학생이 내려받는 파이썬
프로젝트 안의 `assets/`는 **같은 파일이지만 서로 다른 두 개의 사본**입니다.
TODO 9에서 학생이 직접 업로드한 커스텀 이미지/사운드는 이 웹사이트
사본에는 없고 학생 컴퓨터(또는 연결된 프로젝트 폴더)에만 있습니다 — 그래서
zip에는 자동으로 포함되지 않고, `HOW_TO_RUN.txt`와 export 모달에서 "직접
복사해 넣으세요"라고 안내합니다.

## 11. 학교 컴퓨터 첫 실행 체크리스트

실제로 배포하기 전에 학교 컴퓨터(또는 아무 컴퓨터) 한 대에서 아래를 한 번씩
직접 눌러보시길 권합니다.

1. **Pyodide 로딩**: 아무 TODO에서나 "Run my code"를 처음 눌렀을 때 로딩
   배너가 뜨고, 잠시 후 정상적으로 결과가 나오는지.
2. **오답이 멈추지 않고 애니메이션되는지**: TODO 5를 일부러 `pass`로 두고
   dijkstraFrontier 시각화를 눌렀을 때, 무한 루프에 빠지지 않고 정상적으로
   "아직 완성되지 않음" 메시지가 나오는지.
3. **캡스톤 잠금 배너**: TODO 15로 이동했을 때, 다른 Bonus를 하나라도 안
   끝낸 상태에서는 잠금 배너("Finish every other Bonus challenge
   first...")가 보이고, 나머지를 전부 완료/건너뛰기 하면 정상적으로
   풀리는지.
4. **폴더 연결(Connect my project folder)**: TODO 9에서 실제
   `dijkstra_maze/student` 폴더를 선택하고 권한을 허용했을 때 정상
   연결되는지.
5. **내보낸 zip이 진짜 실행되는지**: "Download my project"로 zip을 받아
   압축을 풀고, `pip install -r requirements.txt` 후 `python main.py`로
   실제 게임(몬스터 포함)이 뜨는지 — **가장 중요한 최종 확인**입니다.
6. **진행 상황 export/import 왕복**: "Save my work"/"Load my work" 왕복이
   코드/완료 상태/맵/업로드 목록을 그대로 복원하는지.

## 12. 교사용 자료

**위치: `c:/Users/손채영/Desktop/kazh/pygame/교사용/`** (이 `dijkstra_maze_web/`
폴더 **바깥**입니다). 한국어로 세 파일을 준비했습니다:
- `정답_해설.md` — 15개 TODO 전체 정답 해설(개념, 자주 하는 실수, 채점기가
  허용하는 다른 정답, 힌트 3단계).
- `수업_운영안.md` — 단계별 예상 소요 시간(Required 총 45~60분 목표),
  막히기 쉬운 지점, 캡스톤 잠금/몬스터 Bonus 소개 포인트, Skip 활용법,
  제출물 수거 방법, 채점 기준안.
- `체크리스트.md` — `student/TODO_CHECKLIST.md`와 같은 형식의 15개 TODO
  한 페이지 표.

**학생에게는 `dijkstra_maze_web/` 폴더만 전달하세요** (`교사용/`은 별도
보관).
